# P2.6-11 — Opt-in Deploy and Soak — Review

**Task:** [p2.6-11-optin-deploy-soak.md](../../../plan/wip/phase2.6-adaptive-stage1/p2.6-11-optin-deploy-soak.md) ·
**Plan:** [p2.6-11-optin-deploy-soak-plan.md](../../../plan/wip/phase2.6-adaptive-stage1/p2.6-11-optin-deploy-soak-plan.md) ·
**Spec:** [S1-G2, S1-G4, S1-G5](../../design/adaptive-upstream-selection.md) ·
**Method:** [benchmarks §S1-M, §S1-L](../../design/adaptive-upstream-selection-benchmarks.md) ·
**Harness:** [p2.6-13](p2.6-13-measurement-harness-review.md) ·
**Threshold:** [p2.6-10](p2.6-10-null-ab-review.md) ·
**Audit:** [phase2.6-audit.md](phase2.6-audit.md) — independent recomputation
from raw data, findings F1–F11, and the pre-declared F2 ruling on the W3
gate failure (2026-08-29, day 5 of the soak)

**Status: IN PROGRESS.** This file exists before any measurement because two of
its arms require a written pre-declaration that cannot be added afterwards. The
Implementation Summary is completed when the task is, and the Findings section
stays empty until `start code review`.

## Pre-declarations

Written **2026-08-24**, before the first measurement run of either arm. Nothing
below is revised after a result is seen. A defect found mid-session stops that
session and is reported as a defect; it is not repaired by changing the
declaration.

Carried forward unchanged from `p2.6-10` and not reopened here: the frozen
S1-N measurement protocol (60 s settle, T0, load, **30 s quiet drain**, T1,
perf pull, 60 s cool-down), the S1-G2 tier-3 threshold of **5.00 %** on total
upstream attempts, the fixed metric order, and the descriptive-only status of
`forward` p99 and `cache_hit` p99.

### M.8 — `upstream/forward_udp_answered{,_adaptive}`

This is the execution `p2.6-08` F1 names as its closure condition. F1 is open
because that session's K was extended 3 → 6 mid-run; the declaration below is
the remedy and is worthless if amended later.

| Item | Declared value |
| --- | --- |
| K | **8 pairs**, plus one discarded warm-up pair. The rule requires ≥ 6; 8 matches `p2.6-10`'s K and exceeds `p2.6-08`'s 6 |
| K extension | **Forbidden.** The session runs exactly 8 measured pairs. If 8 prove too few to satisfy the rule, that is the reported outcome, not a reason to add pairs |
| One pair | One process: `cargo bench -p fah-dns --bench upstream -- upstream/forward_udp_answered`, which measures `fallback` then `adaptive` back to back in that process |
| Ordering | `fallback` first, `adaptive` second, in every pair — fixed by `STRATEGIES` in [upstream.rs:12-15](../../../crates/fah-dns/benches/upstream.rs#L12-L15). Alternating the within-process order would be a code edit, which this task does not make. The asymmetry is stated with the result, as `p2.6-08` stated it |
| Warm-up | Pair 0, **discarded**, declared discarded here and reported anyway |
| Duration | **`--sample-size 200`**, criterion's default 3 s warm-up and 5 s measurement otherwise — the session settings `p2.6-08` used |
| Pinning | `ProcessorAffinity = 4` (one core), `PriorityClass = High`, as `p2.6-08` |
| Post-change build | `1c430aa`. `p2.6-08` measured `f28df86`; the commits between them are the `p2.6-09` test suite, documentation, and the version bump — no change on the path M.8 exercises |
| Control | The pre-change checkout `p2.6-08` used (`786e8c8`), `fallback` arm only — it has no `adaptive` twin — same K. Session means must agree within **±2 %** or the session is discarded whole |
| Acceptance | The corrected three-clause rule, unchanged: per pair `d_i = (adaptive − fallback) / fallback`; (1) two-sided 95 % t-CI of `mean(d_i)` contains 0; (2) that CI's upper bound < +10 %; (3) no individual pair exceeds +10 % |
| Descriptive only | Criterion's per-pair within-run CIs and their overlap count; the `fallback` arm's own between-run range |
| Stopping conditions | The session ends after pair 8. It ends early only on a methodology defect, which is reported unfixed. No pair is re-run, and no pair is dropped for looking wrong |

**One correction, made before the first measurement.** The declaration as first
written said "criterion defaults — 100 samples per arm, unchanged from
`p2.6-08`". Those two clauses contradict each other: `p2.6-08` ran
`--sample-size 200`. The error was transcription, not a decision, and it was
caught while setting the session up, with no measurement taken and no result
seen. Corrected to 200 with the owner's agreement, which keeps this session
comparable to the one the M.8 rule judges it against. Recorded here rather than
silently fixed, because a pre-declaration that can be quietly edited is not a
pre-declaration.

M.8 is reported in its **own acceptance table**, separate from every
probe-container arm. It is a dev-box criterion bench and shares neither its
device nor its workload with L.1/L.2/L.4/L.1s.

### L.1 / L.2 — tier 2 and tier 3

| Item | Declared value |
| --- | --- |
| K | **6 repetitions**, 3 per arm, plus one discarded warm-up |
| Design | **Blocked**: A A A, one strategy flip, B B B. Not alternating — the arms differ in `strategy`, so every flip is an owner-run router command, and 5 of them interleaved with 12-minute runs is not a cost this comparison justifies. p2.6-10 measured session drift at **0.1094 %** against a **5.00 %** threshold, so blocking cannot plausibly move the verdict. Stated as the weaker design it is |
| Arm A = L.1 | `adaptive`, probe container, `envlists=fah-env,probe-optin` |
| Arm B = L.2 | `fallback`, probe container, `envlists=fah-env` |
| Duration | 600 s load per repetition, the benchmarks doc's L.1 duration |
| Offered load | 10 000 forward + 1 000 control QPS |
| Protocol | The frozen p2.6-10 repetition, unchanged — 60 s settle, T0, load, 30 s drain, T1, perf pull |
| Restarts | One, at the flip. Counters are T0/T1 deltas, so the reset does not corrupt them |
| Tier 3 | Total upstream attempts, arm means, against the frozen **5.00 %** |
| Stopping | 6 repetitions. No re-runs, no drops, no K extension |

### L.1s — the SWR arm

L.1 produces **zero** SWR activity by construction: its forward names are
unique and never re-queried, so no entry is ever hit while stale. Its `swr.*`
zeroes are therefore a property of the workload, and no statement about SWR
safety may be inferred from them. B.8 (`p2.6-09`) exercised the path in-process
at 100 stale keys against a black-holed endpoint; it says nothing about
on-device behaviour at load. L.1s exists to close that gap, and only that gap.

| Item | Declared value |
| --- | --- |
| Container | The probe container `fah-probe`, started through the `p2.6-13` procedure. Production untouched |
| Arms | `adaptive` and `fallback`, alternating, same session, no restart between repetitions |
| K | **6 repetitions** (3 per arm) plus one discarded warm-up |
| Duration | **300 s** of load per repetition, not 600 s. Every L.1s criterion is an exact count or an exact identity, not a percentile, so the extra 300 s buys no resolution |
| Protocol | The frozen `p2.6-10` repetition, unchanged, including the 30 s quiet drain |
| Offered load | **500 QPS over a recurring 6 000-name set + 1 000 QPS control names.** No unique-name flood — see the amendment below |
| Why those numbers | The mock forward zone's TTL is 5 s and the probe runs `min_ttl_seconds = 0`, so answers expire on schedule. At 500 QPS over 6 000 names each name is re-queried every 12 s, past expiry, so every hit after the first serves stale and enqueues a refresh. Steady-state refresh rate ≈ 500/s. The working set of 6 000 sits below `max_entries = 10 000`, so nothing is evicted before it can go stale |

**Amendment, made before the first L.1s run (2026-08-25).** The declaration
first said 9 500 QPS of unique forward names *plus* the recurring set, totalling
11 000 QPS. That is structurally incapable of measuring SWR: the probe's cache
holds **10 000 entries**, so 9 500 unique names per second evict the whole cache
roughly once a second, and a name re-queried every 12 s is always gone before it
can go stale. The arm would have reported `swr.* == 0` — the same zeros L.1
produces — and the precondition `swr.completed > 0` would have failed for a
reason having nothing to do with SWR.

Found by reading the probe's live `dns.cache` settings while setting the arm up,
before any measurement. The unique-name stream is dropped; the control stream
stays. The owner agreed to the amendment before the first run. Recorded here for
the same reason as the M.8 correction: a declaration that can be quietly edited
is not a declaration.
| Calibration | **One 120 s calibration repetition `C`, discarded, declared discarded here and reported anyway.** `swr_workers = 3` has a finite refresh throughput and the enqueue rate above is an estimate, not a measurement. `C` measures the achieved refresh rate and whether `swr.dropped` moves |
| Rule after `C` | Mechanical, declared now so it is not a judgment call later: if `C` shows `swr.dropped == 0`, the measured repetitions run at the declared rate. If `C` shows `swr.dropped > 0`, the SWR generator's rate is set to **60 % of the enqueue rate at which `C` first dropped**, that saturation figure is reported as a finding about `swr_workers = 3`, and the measured repetitions run at the reduced rate. The client and control rates never change |
| Reported per repetition | `swr.enqueued`, `swr.completed`, `swr.failed`, `swr.dropped`, `upstreams[].attempts` per endpoint, `cache` hits / misses / stale hits, the client-visible `forward` count, `penalties`, `probes` |
| SWR vs client separation | Through the identity `Σ attempts − (swr.completed + swr.failed) == cache_misses`. Client-visible forwards are the `forward` stage count; SWR upstream work is the `swr.completed + swr.failed` term. L.1 never exercised that subtraction because both terms were 0 |

**Does SWR need its own acceptance criterion?** Yes — decided here, before the
measurement. The tier 2 rows `swr.failed == 0` and `swr.dropped == 0` already
exist in the spec but are vacuous under L.1, where nothing enqueues. Passing
them on zeroes is not evidence. L.1s therefore carries its own table, and the
arm reports **no result at all** unless `swr.completed > 0`.

| L.1s row | Type | Criterion |
| --- | --- | --- |
| `swr.completed > 0` | Precondition | Below it the arm did not exercise SWR and reports nothing |
| `swr.failed` | Pass/fail | `== 0` in both arms — the mocks never fail |
| `swr.dropped` | Pass/fail | `== 0` in both arms at the rate fixed by `C` |
| Attempts identity | Pass/fail | `(Σ attempts − (swr.completed + swr.failed)) / cache_misses == 1.000` in both arms |
| `penalties`, `probes` | Pass/fail | Deltas `== 0` on the `adaptive` arm. Refresh traffic against healthy mocks must neither penalize an endpoint nor claim a probe |
| Queue residue | Bounded report | `swr.enqueued ≥ completed + failed + dropped`; the residue is stated, not gated |
| SWR share of attempts | Descriptive | Stated beside the 69.1 % deployed attribution. L.1s is not built to reproduce that share |

**L.1s counts are not pooled into tier 3.** It is a different workload from
L.1, and the 5.00 % threshold and the metric order stay exactly as `p2.6-10`
froze them. L.1s carries no timing gate.

### L.4a / L.4b — the failure-injection arms

Written **2026-08-25**, before the addresses were confirmed and before either
arm ran. The acceptance rows are the benchmarks doc's S1-L rows, copied, not
restated in weaker words.

| Item | Declared value |
| --- | --- |
| Container | `fah-probe`, started through the `p2.6-13` procedure. Production keeps soaking, untouched |
| Strategy | **`adaptive` in both arms** (`envlists=fah-env,probe-optin`). Penalties do not exist under `fallback`, so a `fallback` arm cannot test the classification these arms exist to test |
| Fallback control | **None, deliberately.** L.4's acceptance is behavioural — the B.1 shape and the path-failure row — not a cost comparison, and a control arm would add 2 h for a figure the `adaptive` counters already yield analytically. The saving is reported as descriptive, below |
| Config | The probe TOML gains **one** `[[dns.upstreams.servers]]` entry at the dead address, placed **first** in config order, so one-pass first-healthy-wins tries it on every query until it penalizes. The two mock entries follow, unchanged |
| Schema validation | The new entry is validated against `git show <deployed-tag>:crates/fah-config/src/schema/…` for the **0.2.20** image the probe will run, never against the working tree. This is `p2.6-13` F3's method, not just its fix |
| Mocks | CoreDNS 1.14.7 on the dev box, the same binary and host that served L.1/L.2/L.1s |
| Duration | **1 h per arm**, the benchmarks doc's figure. One run each |
| Offered load | **Strictly sequential — one query in flight**, unique names. B.1's criterion counts queries dispatched *after* the penalty landed; concurrent in-flight queries also pay (design S1.3) and would blur the count. Achieved rate is reported, not targeted |
| Penalty windows in 1 h | Ladder 24/48/96/192/300 s ⇒ ~13 windows, so "one probe carrier per window" is decided on ~13 observations, not one |
| Sampling | `/api/v1/telemetry` polled at **250 ms** for the first 60 s of each arm, then the frozen T0/T1 delta protocol. The probe's 30 s perf cadence cannot resolve the first penalty, and the first penalty is the whole of L.4b |
| Reported per arm | Per endpoint: `attempts`, `failures`, `penalties`, `probes`, `state`; `forward` p50/p99; cache misses; the polled samples spanning the first `penalties` 0 → 1 transition |
| Stopping | One run per arm. No re-runs, no second address, no K extension |

**The addresses are confirmed by the owner before either arm runs**, and the
confirming commands and their output go in the results section. An address that
cannot be shown dead is not used.

| Arm | Address shape | Confirmation |
| --- | --- | --- |
| L.4a | Routable WAN address, nothing answers, **no ICMP unreachable returned** | `/ping` shows timeouts, not "host unreachable". A rejecting address is a path failure and stops being a black hole |
| L.4b | Unused address **inside the LAN prefix**, off-link for the probe | Outside the DHCP pool, no lease, no ARP entry, nothing bound |

**Amendment, made 2026-08-25 before either arm ran.** L.4b's row first read "a
prefix the probe ARPs into", which is narrower than the benchmarks doc's "an
unused address inside the LAN prefix" and picks a different mechanism. The probe
sits on `172.17.0.0/24`; an unused address *there* is on-link, so the container's
own kernel queues the packet and reports `EHOSTUNREACH` only after its ARP
retries expire — seconds, against `timeout_ms = 800`. It would classify as a
timeout for a reason having nothing to do with path failure. An unused address in
`192.168.10.0/24` is off-link for the container: the router's ARP fails and its
ICMP host-unreachable reaches the probe in milliseconds. Same amendment rule as
L.1s — declared before any measurement, recorded with its reason. **No address is
changed after a result is seen.**

| Arm | Address | Evidence, owner-run 2026-08-25 |
| --- | --- | --- |
| L.4a | `198.51.100.53` | `/ping count=5` → `sent=5 received=0 packet-loss=100%`, every row `timeout`, no `host unreachable`. Default route `0.0.0.0/0` via `DIGI` active, so the packets leave |
| L.4b | `192.168.10.9` | **Below the DHCP pool** (`dhcp` = `.10`–`.254`), so DHCP cannot assign it mid-run. No lease, no ARP entry. The 12 leases occupy `.10`–`.21`; `.14` was rejected despite its `failed` ARP status — that host left and can return |

| Row | Type | Criterion |
| --- | --- | --- |
| L.4a shape | Pass/fail | `penalty_failures` non-probe queries pay, then **one** probe carrier per penalty window, on real hardware |
| L.4a `attempt_bound_ms` | Report | The cap may be exercised here where S1-B's UDP mocks cannot. Stated, not gated |
| L.4b classification | Pass/fail | Penalized after **exactly 1 attempt** with `failures == 1` on the dead entry and `penalty_round == 1` |
| L.4b corroboration | Supporting | Wall-clock to the first penalty ≈ ms (path failure) vs ≈ 1.6 s (two 800 ms timeouts). Counters decide; latency corroborates |
| Saving vs `fallback` | Descriptive | Derived from the `adaptive` counters — queries × `timeout_ms` not paid. Not a gate, and not pooled with tier 3 |

**If L.4b's kernel reports a timeout instead of `EHOSTUNREACH`**, the arm has
measured the L.4a shape a second time. It then says so and the path-failure row
is recorded **unconfirmed on this platform** — the outcome the benchmarks doc
already names. A second address is *not* tried: picking addresses until one
classifies the declared way is fishing, and it would turn a measurement into a
search for a pass.

**L.4 counts are not pooled into tier 3.** Different workload; the frozen
5.00 % threshold and the metric order stay as `p2.6-10` left them.

## Merge gate — dev box, 2026-08-24

Checked before any deploy proposal is executed. **All three inputs hold.**

### Workspace gates (S1-G1 #1)

| Gate | Result |
| --- | --- |
| `cargo fmt --all -- --check` | clean |
| `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| `cargo test --all-features --workspace` | exit 0 — **1 056 passed, 0 failed, 8 ignored** |

The `WSAEACCES` (10013) e2e failure this box produces when WinNAT reserves the
ephemeral port block did not occur in this run.

### S1-G1 #1–#17

Coverage was established in the `p2.6-08` cross-task audit and is restated here
with the tests that carry each gate. Nothing was re-derived; the gate is that
all of them are green in the run above.

| # | Carried by | Origin |
| --- | --- | --- |
| 1 | the three gates above | — |
| 2 | `crates/fastadhunter/tests/layering.rs` | p2-13 |
| 3 | `const` size assert, `health.rs:50` | p2.6-02 |
| 4 | `fallback` arm diffed line for line against `0159cf7~1`; the only change is `server.attempts`/`.failures` → `health[i]` | p2.6-05 |
| 5 | `an_rcode_answer_classifies_as_a_transport_success` + B.6 | p2.6-04, p2.6-09 |
| 6 | `all_penalized_forces_the_earliest_deadline_and_stores_nothing`, `every_endpoint_penalized_still_sends_a_packet_and_claims_no_probe` | p2.6-03, p2.6-05 |
| 7 | `resolve_host_with_one_family_black_holed_moves_no_health_state` | p2.6-05 |
| 8 | `exactly_one_thread_claims_the_probe_{with_no_other_endpoint,ahead_of_a_healthy_endpoint}`, `concurrent_queries_claim_exactly_one_probe` | p2.6-03, p2.6-05 |
| 9 | `probing_failure_penalizes_deeper` | p2.6-02 |
| 10 | `next_round_resets_only_after_continuous_healthy_max` | p2.6-02 |
| 11 | `concurrent_saturated_failures_leave_word_intact`, `sequential_probe_failures_keep_round_saturated` | p2.6-02 |
| 12 | `penalized_success_restores_healthy_round_kept` | p2.6-02 |
| 13 | `healthy_hard_failure_below_threshold_counts_only` | p2.6-02 |
| 14 | `an_idle_close_and_reconnect_moves_no_health_under_adaptive` | p2.6-05 |
| 15 | `status_follows_a_probe_through_failure_and_recovery` | p2.6-06 |
| 16 | `resolve_host_never_claims_a_probe_on_a_due_endpoint`, `without_claim_a_due_endpoint_is_skipped_and_untouched` | p2.6-03, p2.6-05 |
| 17 | `one_query_claims_at_most_one_probe_across_two_due_endpoints` | p2.6-05 |

### Tier-1 and G3 result files

| Input | State |
| --- | --- |
| S1-G2 tier 1 — [p2.6-08](p2.6-08-microbench-review.md) | **PASS WITH DEFERRED FINDINGS.** M.1–M.7, M.9 within budget; M.8 satisfies all three clauses. F1 open by construction and closed by this task |
| S1-G3 — [p2.6-09](p2.6-09-injected-failure-bench-review.md) | **PASS WITH DEFERRED FINDINGS.** Seven of eight arms pass on independently recomputed numbers; B.2 deferred as undecidable on Windows, not failed |
| S1-G2 tier 3 threshold — [p2.6-10](p2.6-10-null-ab-review.md) | Frozen at **5.00 %** on total upstream attempts |

**Merge gate: PASS.** The task may proceed to the deploy proposal.

## Deploy proposal — commands only

**Nothing below has been run.** Every `/container`, `/file` and `/ip` line is a
proposal for the owner. The agent executes no write on the RB5009.

### A trap that must be settled before the opt-in

The production container and the probe container **share the envlist
`fah-env`**. Adding `FAH__DNS__UPSTREAMS__STRATEGY=adaptive` to `fah-env` would
flip *both*, silently turning every future `fallback` arm (L.2, the tier-3
control) into a second `adaptive` arm. The opt-in therefore goes in a **second,
production-only envlist**, and the probe's `envlists` is left untouched.

### Step 1 — read-only pre-flight (owner runs, output pasted back)

```routeros
/container/print detail where comment="fastadhunter"
/container/envs/print
/file/print where name~"kingston"
```

What each line settles:

- `/container/print detail` — the production container's exact `root-dir`,
  `mountlists`, `envlists`, `workdir`, `cpu-list` and `comment`. Step 4's `add`
  reuses them verbatim; they are deployment-specific and are not assumed.
- `/container/envs/print` — that no `fah-optin` list already exists, and what
  `fah-env` currently holds.
- `/file/print where name~"kingston"` — that the **0.2.19 tar is still on the
  store**, and free space for one more extracted rootfs (~15 MiB).

### Step 1 results — owner-run, 2026-08-24

| Field | Value |
| --- | --- |
| `file` | **`kingston/fastadhunter-arm64-0.2.19.tar`** |
| derived `name` | `fastadhunter-arm64-0.2.19` |
| `tag` | `docker.io/library/fastadhunter:0.2.19` |
| `interface` | `veth1` |
| `root-dir` | `/kingston/fastadhunter/root` |
| `mountlists` | `fah-config,fah-data` |
| `envlists` | `fah-env` |
| `env` | empty |
| `workdir` | `/home/nonroot` |
| `logging` | `yes` |
| `start-on-boot` | `yes` |
| `cpu-list` | `""` — no restriction, as `routeros-traps.md` records |
| `memory-high` | `unlimited` |
| `auto-restart-interval` | `none` |
| `shm-size`, `stop-signal`, `stop-time` | `64.0MiB`, `15-SIGTERM`, `10s` — all defaults |
| state | `R` running, `memory-current` 92.1 MiB |

`/container/envs/print` holds **only** `fah-env`, with the three `MIMALLOC_*`
keys. No `fah-optin` exists yet, and `fah-env` carries no `FAH__*` key — so the
production container's strategy comes from `/config/fastadhunter.toml` alone
today.

Tars on the store: `fastadhunter-arm64-0.2.19.tar` (13.1 MiB, the live one),
`fastadhunter-stage1-f65386f.tar` (the probe image),
`fastadhunter-rosready-0.2.18.tar`, `fastadhunter-rosready-0.2.17.tar`.

**Correction this forces.** The rollback commands drafted above named
`fastadhunter-rosready-0.2.19.tar`. **No such file exists** — the live image
was uploaded as `fastadhunter-arm64-0.2.19.tar`, and only 0.2.17 and 0.2.18
carry the `rosready` name. Every rollback reference below uses the real name.
The naming is not consistent on this store and must be read, never inferred.

Two things the pre-flight cannot answer and that are settled by watching Step 4
rather than by guessing: whether RouterOS creates `root-dir=kingston/…/root-0220`
itself on extract, and whether `cpu-list` on production is still `""`. If the
new container never leaves `status=extracting`, the first is the suspect after
the image layout.

### Step 2 — version bump (dev box, needs a separate approval)

Workspace `version` 0.2.19 → **0.2.20** in `Cargo.toml`, committed on its own.
No other code change. Requested when the gate below is approved, not now.

### Step 3 — build and ship (dev box)

QEMU must be re-registered after any Docker Desktop restart, and the builder
container restarted afterwards — `buildx inspect --bootstrap` alone does not
refresh the platform list.

**Deviation, 2026-08-25:** `buildx_buildkit_fah-builder0` no longer exists on
the dev box — `docker restart` returns `No such container`. The named builder
`routeros-traps.md` describes was lost to a Docker Desktop reset at some point,
and with it its build cache. `docker buildx ls` shows `desktop-linux` already
carrying `linux/arm64`, so the build runs there. The consequence is a cold
QEMU-emulated build, not a different artefact.

```sh
docker run --privileged --rm tonistiigi/binfmt --install arm64

docker buildx build \
  --platform linux/arm64 \
  -t fastadhunter:0.2.20 \
  -o type=docker,dest=fastadhunter-arm64-0.2.20.tar \
  .
```

**Then check the layout — not optional.** With the containerd image store on
(the Docker Desktop default) `-o type=docker` still emits OCI, which RouterOS
cannot follow: it hangs at `status=extracting` forever with `OS` and `Arch`
blank, and reports no error. A `manifest.json` in the listing does not mean
legacy; judge by where the layers live:

```sh
tar -xOf fastadhunter-arm64-0.2.20.tar manifest.json
```

`"Layers":["blobs/sha256/…"]` → OCI, convert. `"Layers":["<hash>.tar"]` →
already legacy, skip the conversion.

```sh
docker run --rm -v "$PWD:/work" quay.io/skopeo/stable copy --insecure-policy \
  oci-archive:/work/fastadhunter-arm64-0.2.20.tar \
  docker-archive:/work/fastadhunter-rosready-0.2.20.tar:fastadhunter:0.2.20

ls -lh fastadhunter-rosready-0.2.20.tar
```

Expect ~12 MB — legacy layers are uncompressed. Budget is ≤ 30 MB.

### Step 3 results — dev box, 2026-08-25

| Item | Value |
| --- | --- |
| Version bump | `1c430aa`, `Cargo.toml` + `Cargo.lock` only, nothing else staged. Not pushed |
| Builder | `desktop-linux` — `fah-builder0` no longer exists, see the deviation above |
| buildx output | `fastadhunter-arm64-0.2.20.tar`, 5.7 MB, **OCI layout** — `"Layers":["blobs/sha256/…"]`, 15 layers. RouterOS cannot import it |
| After skopeo | `fastadhunter-rosready-0.2.20.tar`, **13.1 MB**, flat `<hash>.tar` entries at the top level, `"Layers":["6fc9269a….tar", …]` — **legacy, importable** |
| Tag | `docker.io/library/fastadhunter:0.2.20`, matching production's `docker.io/library/fastadhunter:0.2.19` |
| Location | `e:\FastAdHunter\` — `*.tar` is gitignored |

The layout check was not a formality: buildx emitted OCI despite
`-o type=docker`, which is the failure that hangs RouterOS at
`status=extracting` with no error. Both files are kept; only the `rosready` one
is uploaded.

**The 0.2.20 binary is code-identical to the Stage 1 probe image.** The probe
was built at `f65386f`; `HEAD` is that commit plus one documentation commit and
the version bump, with no runtime change between them. This is what makes the
probe container a valid `fallback` control for L.2 and tier 3 — the two arms
differ in configuration, not in code.

The upload is a write and is **requested separately**:

```sh
scp fastadhunter-rosready-0.2.20.tar bobdenaut:kingston/
```

### Step 4 — deploy the binary, still under `fallback`

S1.14 order: binary first, config second. **Add-first, chosen by the owner.**
The old container is kept, stopped, until the new binary has proved itself —
which is what makes the rollback in Step 6 a few seconds rather than a
re-extraction.

The new container gets its own `root-dir` and a comment that is **not**
`fastadhunter`: `[find comment="fastadhunter"]` is exact equality and would
otherwise match both. RouterOS auto-suffixes the derived name, so the shared
image tar causes no collision.

Every field below is copied from Step 1's `print detail`, with three deliberate
differences: `root-dir`, `start-on-boot` and `comment`.

```routeros
/container/add file=kingston/fastadhunter-rosready-0.2.20.tar interface=veth1 \
  root-dir=/kingston/fastadhunter/root-0220 mountlists=fah-config,fah-data \
  envlists=fah-env workdir=/home/nonroot start-on-boot=no logging=yes \
  comment="fah-next"
```

`shm-size`, `stop-signal`, `stop-time`, `cpu-list`, `memory-high` and
`auto-restart-interval` are left unset because production runs them at their
defaults. `memory-high` stays **unlimited** — a value there once OOM-killed the
live resolver.

Wait for extraction to finish before stopping anything — `status=extracting`
that never becomes `stopped` is the OCI-layout failure from Step 3, and the
old container must keep serving if it appears:

```routeros
/container/print where comment="fah-next"
```

Only once it reads `status=stopped`:

```routeros
/container/stop [find comment="fastadhunter"]
/container/start [find comment="fah-next"]
```

Both containers use the same `fah-config`/`fah-data` mount lists and only one
runs at a time. The comment swap and the removal of the old container happen
**after the soak decides**, not now — until then `comment="fastadhunter"` still
names the 0.2.19 container, and every `[find comment="fastadhunter"]` in an
older runbook points at the stopped one.

### Step 4b — `fallback` verification, before any opt-in

```routeros
/log print where topics~"container"
/container/print detail where comment="fah-next"
```

Then, from the LAN:

- `GET /api/v1/config` reports `dns.upstreams.strategy = "fallback"`.
- `GET /api/v1/telemetry` shows the p2.6-06 fields present on `upstreams[]`
  (`state`, `penalty_round`, `penalties`, `penalized_seconds_total`, `probes`,
  `probe_successes`, `family`) — this, not the version string, is what
  distinguishes the 2.6 binary from 0.2.19.
- `state` reads `healthy` on every endpoint, which is what `fallback` always
  reports, and forward traffic resolves.

**A named mount is not a working mount.** Confirm the new `root-dir` is not
quietly holding the writes:

```routeros
/file/print where name~"fastadhunter/root-0220/data"
```

Anything with a size there means `fah-config`/`fah-data` did not attach and the
container is writing into its own store, where `container/remove` will delete
it. Empty stubs dated at image-build time mean the mounts are live.

### Step 4b results — 2026-08-25 00:35 local (21:35 UTC)

`fah-next` started clean on `veth1`: 16 lists restored from cache, ruleset
compiled at **752 647 rules**, DNS bound `[::]:53` udp+tcp, HTTP `[::]:8080`,
API `https://0.0.0.0:8443`, privileges dropped to uid/gid 65532, SWR pool 3
workers. RSS 77.5 MiB against the outgoing container's 90.8 MiB.

| Check | Result |
| --- | --- |
| Mounts live | **Pass.** `/file/print where name~"kingston/fastadhunter/root-0220/data"` returns nothing, and the boot log read the 16 cached lists out of `/data` |
| `strategy` | **`"fallback"`** — binary changed, config did not |
| p2.6-06 telemetry fields | **Pass.** All seven present on every endpoint — `state`, `penalty_round`, `penalties`, `penalized_seconds_total`, `probes`, `probe_successes`, `family`. `family` reads `v4`/`v4`/`v6`/`v6` in config order |
| Functional resolve + block | **Pass.** `example.com` resolves A + AAAA through `172.17.0.2`; `doubleclick.net` returns `0.0.0.0` and `::`, matching `blocking.mode = "null_ip"` |
| Endpoint traffic distribution | `1.1.1.1` 469 attempts, the other three **zero** — the known `fallback` behaviour, and the reason S1-G4's window is thin |

**Step 4b: PASS.** The 0.2.20 binary is healthy under `fallback`.

**Deviations from what the plan file assumed, both read from the live config:**

- **`history.sample_interval_seconds = 360`, not 60.** The plan's perf-history
  arithmetic, the S1-G4 `failure_runs` reading and the L.3 false-penalty
  definition were all written against a 60 s sample. The real cadence is six
  times coarser: **240 rows/day**. The false-penalty rule becomes "a
  `penalties` delta in a **360 s** sample where the other endpoints' `failures`
  did not move", which is a weaker attribution — six minutes is long enough for
  an unrelated endpoint to move inside the same row. Stated as a limit on G4
  row 5, not worked around. **Owner ruling, 2026-08-25: this is evaluated as a
  limit on G4's evidence and is not repaired by retrofitting the protocol.** The
  soak's samples were recorded at 360 s; restating the definition afterwards to
  fit a cadence the data does not have would be a declaration edited after the
  result.
- **Disk growth is smaller than calculated, for the same reason.** The plan
  says ~123 B per endpoint per sample, 0.35 MB/day at two endpoints on a 60 s
  cadence. At four endpoints on 360 s it is `123 × 4 × 240` ≈ **118 KB/day**,
  ~3.5 MB over the 30-day retention. Still measured across the soak rather than
  asserted — the calculated figure is the bound a measurement can exceed.
- **Four upstreams, not two**: `1.1.1.1`, `9.9.9.9`, `2606:4700:4700::1111`,
  `2620:fe::fe`. The open unknown that three of the four produce no data under
  `fallback` applies directly to S1-G4's window.
- `penalty_failures = 2` appears in the API response, which is the serialized
  config and not proof of what the file holds. Whether
  `/config/fastadhunter.toml` now carries the key is checked by reading the
  file, and it decides whether the rollback edit is mandatory.

### Step 4c — hand boot ownership to `fah-next`

Runs immediately after Step 4b passes, before the opt-in. `fah-next` was added
with `start-on-boot=no`, which is right while 0.2.19 is still serving and wrong
for the seven days after. An unattended router reboot inside a 7-day soak is
more likely than a rollback, and with both flags left as they are it would come
back with **no resolver at all**:

```routeros
/container/set [find comment="fah-next"] start-on-boot=yes
/container/set [find comment="fastadhunter"] start-on-boot=no
```

`/container/set` on a stopped container is a definition change and needs no
restart; on the running `fah-next` it takes effect at the next boot, which is
the point. Confirm both:

```routeros
/container/print detail where comment="fah-next"
/container/print detail where comment="fastadhunter"
```

**Step 4c results — confirmed 2026-08-25.** `fah-next` `start-on-boot=yes`,
`fastadhunter` `start-on-boot=no`. Both containers otherwise identical field
for field except `file`, `root-dir`, `layers` and `comment`.

`fah-next` carries `image-id=5845c30a7135…`, which is the config blob sha256
from the OCI manifest of the tar built in Step 3. The running image is
provably the artefact built from `1c430aa`, not an older upload.

### Naming from this point on

The old container **keeps** `comment="fastadhunter"` for the whole soak — a
rename buys nothing and costs a step that can fail. The consequence is a
selector trap, and it is the reason the rule below is absolute rather than a
preference:

> From Step 4 onward, every soak, verification and rollback command targets the
> live container as **`[find comment="fah-next"]`**. `[find
> comment="fastadhunter"]` now resolves to the **stopped 0.2.19 rollback
> target**, and any older runbook line carrying it is pointing at the wrong
> container.

The comment swap happens only in the post-soak cleanup, once the old container
is removed and the selector is unambiguous again.

### Step 5 — the opt-in, as a separate change

Run **only after** Step 4b's `fallback` verification has passed. The target is
`fah-next`, the container now serving; `fah-env` is not edited, so the probe
keeps its `fallback` configuration and stays usable as the L.2 and tier-3
control.

```routeros
/container/envs/add name=fah-optin key=FAH__DNS__UPSTREAMS__STRATEGY value=adaptive
/container/stop [find comment="fah-next"]
/container/set [find comment="fah-next"] envlists=fah-env,fah-optin
/container/start [find comment="fah-next"]
```

Confirmed only by `GET /api/v1/config` reporting
`dns.upstreams.strategy = "adaptive"`. Until it does, the plan does not assume
the opt-in happened. `penalty_failures` stays at its compiled default of 2
unless S1-G4 says otherwise.

Independent confirmation, not a second reading of the same field:
`GET /api/v1/telemetry` must now show `state` capable of leaving `healthy` and
the `penalties`/`probes` counters live. Under `fallback` `state` is hard-wired
to `healthy` (p2.6-06), so a `fallback` container cannot produce a non-healthy
`state` no matter what happens upstream.

### Step 6 — rollback to 0.2.19

**The config edit comes first and is mandatory.** The 0.2.20 binary serializes
every field, so first boot and any `POST /api/v1/config` write
`penalty_failures = 2` into `/config/fastadhunter.toml`. 0.2.19 rejects that key
under `serde(deny_unknown_fields)` and **will not boot** — the same failure that
killed the first probe container in `p2.6-13`.

Add-first makes this cheap: the 0.2.19 container is still present, still
`comment="fastadhunter"`, already extracted, merely stopped. No re-add, no tar,
no extraction wait.

1. Fetch `/kingston/fastadhunter/config/fastadhunter.toml` by `scp` (RouterOS
   `/file get` cannot edit it) and check whether `penalty_failures` is in it.
   If the key is there, delete the line and write the file back. Both transfers
   are owner-run.

   **Checked 2026-08-25, after the 0.2.20 boot: the key is NOT present.** The
   file still reads `strategy = "fallback"` / `timeout_ms = 800` with nothing
   appended. 0.2.20 writes the key only on first-boot generation — which did
   not happen, the file already existed — or on a `POST /api/v1/config`, which
   nothing has issued. **Rollback is currently a two-command operation with no
   config edit.**

   This is a property of the current state, not of the build, and it can change
   without anyone intending it: **any** `POST /api/v1/config` during the soak —
   from the dashboard, from a script, from `tui-monitor` if it ever writes —
   rewrites the whole file and arms the trap silently. The check is therefore
   repeated immediately before any rollback, never inherited from this reading.
2. Then:

```routeros
/container/stop [find comment="fah-next"]
/container/set [find comment="fastadhunter"] start-on-boot=yes
/container/set [find comment="fah-next"] start-on-boot=no
/container/start [find comment="fastadhunter"]
```

The two `set` lines undo Step 4c and are **part of the rollback, not a
follow-up**. Leaving them out produces a resolver that works until the next
reboot and then does not come back — the failure Step 4c exists to prevent,
reintroduced by rolling back without reversing it.

The old container never carried `fah-optin`, so nothing has to be unset. If the
config file was left carrying `penalty_failures`, 0.2.19 does not boot — it
exits at config parse with `unknown field 'penalty_failures'`, exactly as the
first probe container did in `p2.6-13`. That is the whole reason step 1 comes
first.

Only after the soak has decided, and never during it, the cleanup:

```routeros
/container/remove [find comment="fastadhunter"]
/container/set [find comment="fah-next"] comment="fastadhunter"
```

`start-on-boot` is not touched here — Step 4c already moved it to `fah-next`.
The removal comes first so that the rename cannot briefly leave two containers
answering to `comment="fastadhunter"`.

### What "rollback verified" can and cannot mean here

Verifying the rollback by executing it costs a second DNS outage on the
household resolver for no measurement gain. What is verified instead, before
the soak starts, is every precondition it depends on:

| Precondition | How it is checked |
| --- | --- |
| The 0.2.19 container still exists, stopped, with its `root-dir` intact | `/container/print detail where comment="fastadhunter"` after Step 4 |
| The 0.2.19 tar is still on the store — the fallback to the fallback, if that container is ever removed | **Confirmed**, `kingston/fastadhunter-arm64-0.2.19.tar`, 13.1 MiB |
| Whether the live config actually contains `penalty_failures` | **Checked after the 0.2.20 boot — absent.** Re-checked before any actual rollback, because a single `POST /api/v1/config` re-arms it |
| 0.2.19 rejects that file, and accepts it once the line is gone | Reproduced **on the dev box** against the 0.2.19 binary, not on the router |

Stated as a limit, not as a pass: the rollback path is verified by
precondition, and has not been executed end to end on the device. Executing it
would cost a second outage on the household resolver and produce no
measurement.

## M.8 — results

Executed 2026-08-25 on the dev box, exactly as pre-declared: 8 measured pairs
plus the discarded warm-up, `fallback` then `adaptive` back to back in one
process per pair, `ProcessorAffinity = 4`, `PriorityClass = High`,
`--sample-size 200`. Post-change `1c430aa`; control from a `git worktree` at
`786e8c8`, `fallback` arm only, one process per pair. K was not extended and no
pair was re-run or dropped.

Device: 13th Gen Intel Core i9-13980HX, Windows 11 IoT Enterprise LTSC 26100,
rustc 1.96.0, `bench` profile.

### Raw estimates — every pair, none discarded

Criterion `[lower estimate upper]` in µs. `d_i = (adaptive − fallback) / fallback`.

| Pair | post `fallback` | post `adaptive` | control `786e8c8` | `d_i` | CIs overlap |
| --- | --- | --- | --- | --- | --- |
| W (discarded) | `[77.185 77.582 78.023]` | `[77.017 77.484 77.993]` | `[83.042 83.645 84.238]` | −0.13 % | yes |
| 1 | `[87.352 87.957 88.603]` | `[83.113 83.637 84.218]` | `[77.392 77.655 77.964]` | **−4.91 %** | no — `adaptive` faster |
| 2 | `[77.786 77.977 78.179]` | `[77.930 78.134 78.351]` | `[79.556 79.815 80.085]` | +0.20 % | yes |
| 3 | `[79.612 79.954 80.309]` | `[79.127 79.392 79.662]` | `[83.750 84.143 84.572]` | −0.70 % | yes |
| 4 | `[78.865 79.152 79.464]` | `[78.941 79.274 79.679]` | `[88.994 89.405 89.804]` | +0.15 % | yes |
| 5 | `[77.946 78.202 78.471]` | `[77.696 77.921 78.165]` | `[77.785 78.020 78.265]` | −0.36 % | yes |
| 6 | `[78.326 78.511 78.703]` | `[81.033 81.991 83.008]` | `[78.291 78.513 78.756]` | **+4.43 %** | no — `adaptive` slower |
| 7 | `[78.764 79.008 79.255]` | `[79.177 79.390 79.607]` | `[78.870 79.091 79.316]` | +0.48 % | yes |
| 8 | `[78.532 78.834 79.159]` | `[77.609 77.837 78.066]` | `[85.207 85.681 86.138]` | **−1.26 %** | no — `adaptive` faster |

### Derived statistics

`mean(d_i) = −0.2459 %`, `s = 2.5581 %`, `SE = 0.9044 %`, `t₀.₉₇₅,₇ = 2.365`.

**Two-sided 95 % t-CI of `mean(d_i)`: `[−2.385 %, +1.893 %]`.**

### Acceptance — the corrected three-clause rule

| Clause | Measured | Verdict |
| --- | --- | --- |
| 1. 95 % t-CI of `mean(d_i)` contains 0 | `[−2.385 %, +1.893 %]` | **PASS** |
| 2. CI upper bound < +10 % | **+1.893 %** | **PASS** |
| 3. No individual pair exceeds +10 % | worst pair **+4.43 %** (pair 6) | **PASS** |
| Pre-condition — K pre-declared before the first measurement | K = 8, written before the session; the one correction (`--sample-size`) was made and recorded before any run | **MET** |
| Control — session means within ±2 % of the pre-change checkout | post `fallback` 79.949 µs vs control 81.540 µs → **−1.95 %** | **PASS, marginally** |

**M.8: PASS on all three clauses.**

### Descriptive data — not the gate

| Arm | mean of 8 | between-run range | drift |
| --- | --- | --- | --- |
| post `fallback` | 79.949 µs | 77.977–87.957 | 12.80 % |
| post `adaptive` | 79.697 µs | 77.837–83.637 | 7.45 % |
| control `786e8c8` | 81.540 µs | 77.655–89.405 | 15.13 % |

Per-pair CI overlap: **5 of 8**. Of the three non-overlaps, two favour
`adaptive` and one is adverse. `p2.6-08` measured the null baseline for this
estimator — identical code rejected in 67–83 % of comparisons — so 5-of-8
overlap is better than what identical code produces. The estimator is the
defect, not the implementation: criterion's ±0.3–1 % within-run interval does
not contain the 7–15 % between-run drift above, which is exactly why the gate
is the pooled t-CI and not per-pair overlap.

Two observations that are honest limits rather than results:

- **The control margin is thin.** −1.95 % against a ±2 % rule is a pass by
  0.05 points. `p2.6-08` measured **+1.86 %** on the same comparison — same
  magnitude, opposite sign, which is the signature of session drift rather than
  a real difference between checkouts. The rule is met; the margin is not
  comfortable, and a future session could fail it on noise alone.
- **Pair 1 and pair 6 dominate the variance.** They contribute 43.7 of the
  45.8 total sum of squares. Drop either and the CI narrows by roughly half —
  which is precisely why neither was dropped.

### p2.6-08 F1

**CLOSED.** F1's stated closure condition was that the next M.8 execution
pre-declare K in writing before the first measurement. K = 8 was written into
this file, with ordering, warm-up, duration, control and stopping rules, before
any bench ran. The single amendment — `--sample-size` 100 → 200 — was made
before the first measurement, with the owner's agreement, and is recorded above
with its reason rather than silently applied.

## L.1 / L.2 — results

Executed 2026-08-25 on the probe container, exactly as declared: warm-up
discarded, 3 repetitions per arm, 600 s load at 10 000 forward + 1 000 control
QPS, frozen p2.6-10 repetition with the 30 s drain. Blocked design, one restart
at the flip. Every counter below is a T1 − T0 delta.

Mocks: native CoreDNS 1.14.7 on the dev box, `192.168.10.10:5301/5302`,
Corefiles byte-identical to the committed
[p2.6-10-session/config/](p2.6-10-session/config/) copies.

### Every repetition, none discarded

| Rep | Arm | Upstream attempts | failures | penalties | probes | attempts/miss |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `adaptive` | 6 038 819 | [0, 0] | [0, 0] | [0, 0] | 1.000000 |
| 2 | `adaptive` | 6 038 550 | **[1, 0]** | [0, 0] | [0, 0] | 1.000000 |
| 3 | `adaptive` | 6 031 149 | [0, 0] | [0, 0] | [0, 0] | 1.000000 |
| 4 | `fallback` | 6 027 535 | **[1, 0]** | [0, 0] | [0, 0] | 1.000000 |
| 5 | `fallback` | 6 034 060 | [0, 0] | [0, 0] | [0, 0] | 1.000000 |
| 6 | `fallback` | 6 040 426 | [0, 0] | [0, 0] | [0, 0] | 1.000000 |

`swr.failed` and `swr.dropped` are 0 in every repetition, and so is every other
`swr` counter — L.1 exercises no SWR by construction, which is why L.1s exists.

**Two single transport failures occurred**, one per arm, on endpoint `5301`.
Neither produced a penalty: the threshold is 2 *consecutive* failures. In
rep 2 the query fell through to `5302`, which is the one attempt that endpoint
received all session. This is the designed behaviour observed live rather than
asserted, and the symmetry across arms is incidental, not a control.

### S1-G2 tier 2

| Invariant | Measured | Verdict |
| --- | --- | --- |
| `penalties == 0`, both endpoints, both arms | 0 in all 6 repetitions | **PASS** |
| `(Σ attempts − (swr.completed + swr.failed)) / cache_misses == 1.000` | **1.000000** in all 6 | **PASS** |
| `penalties` and `probes` deltas == 0 on the `adaptive` arm | both 0 in reps 1–3 | **PASS** |
| `swr.failed == 0` and `swr.dropped == 0`, both arms | 0 in all 6 | **PASS**, but vacuous — nothing enqueued. Carried by L.1s |
| `swr.completed` reported | 0 in all 6 | Reported |

### S1-G2 tier 3

| Quantity | Value |
| --- | --- |
| `adaptive` mean attempts | 6 036 172.7 |
| `fallback` mean attempts | 6 034 007.0 |
| Delta | **+0.0359 %** |
| Frozen threshold | 5.00 % |

**PASS**, by a factor of 139. The measured delta is a third of the 0.1094 %
noise band p2.6-10 derived, so it is indistinguishable from zero at this
instrument's resolution — the correct reading is "no difference detected", not
"`adaptive` costs 0.036 % more".

Total upstream attempts is workload-determined here — every forward is a cache
miss and each miss makes exactly one attempt — so this row confirms `adaptive`
adds no *extra* upstream work. It is not evidence about latency, and none is
claimed.

**Tier 2: PASS. Tier 3: PASS.**

## L.1s — results

Executed 2026-08-25 on the probe container. Calibration `C` first, then the
`fallback` block (discarded warm-up + 3 repetitions), one strategy flip, then
the `adaptive` block (3 repetitions). 300 s load per repetition at 500 QPS over
the 6 000-name recurring set + 1 000 QPS control, frozen p2.6-10 repetition with
the 30 s drain.

### Calibration `C` — discarded, reported

120 s: `enqueued` 42 051, `completed` 42 051, **`dropped` 0**, `failed` 0;
attempts 45 053 = 3 002 misses + 42 051 refreshes. `dropped == 0` selected the
declared-rate branch of the pre-declared rule, so the measured repetitions ran
at 500 QPS unchanged. No rate reduction was applied and none was needed.

### Every repetition, none discarded

| Rep | Arm | attempts | SWR completed | enqueued | dropped | failed | misses | hits | penalties | probes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| W | `fallback` | 113 593 | 113 590 | 113 590 | **410** | 0 | 0 | 450 002 | [0,0] | [0,0] |
| 1 | `fallback` | 114 001 | 114 000 | 114 000 | 0 | 0 | 0 | 449 999 | [0,0] | [0,0] |
| 2 | `fallback` | 114 000 | 114 000 | 114 000 | 0 | 0 | 0 | 450 002 | [0,0] | [0,0] |
| 3 | `fallback` | 114 000 | 114 000 | 114 000 | 0 | 0 | 0 | 450 002 | [0,0] | [0,0] |
| 4 | `adaptive` | 114 050 | 111 000 | 111 000 | 0 | 0 | 3 050 | 446 952 | [0,0] | [0,0] |
| 5 | `adaptive` | 114 000 | 114 000 | 114 000 | 0 | 0 | 0 | 450 002 | [0,0] | [0,0] |
| 6 | `adaptive` | 114 000 | 114 000 | 114 000 | 0 | 0 | 0 | 450 002 | [0,0] | [0,0] |

380 refreshes/s sustained on three workers. The warm-up's **410 drops** are the
cold-start burst — the first pass over 6 000 uncached names expires together and
enqueues faster than three workers drain. It is reported, not hidden, and it was
declared discarded before the session. Repetition 4 is the same cold start after
the flip's restart and dropped nothing, so the burst is not deterministic.

### Acceptance

| Row | Type | Measured | Verdict |
| --- | --- | --- | --- |
| `swr.completed > 0` | Precondition | 111 000–114 000 per repetition | **MET** — the arm exercised SWR |
| `swr.failed == 0`, both arms | Pass/fail | 0 in all 7 runs | **PASS** |
| `swr.dropped == 0`, both arms at the calibrated rate | Pass/fail | 0 in all 6 measured repetitions | **PASS** |
| Attempts identity | Pass/fail | See below | **PASS** |
| `penalties`, `probes` deltas == 0 on `adaptive` | Pass/fail | 0 in reps 4–6 | **PASS** |
| `enqueued ≥ completed + failed + dropped` | Bounded report | Residue **0** in every repetition; the queue drained completely | Reported |
| SWR share of upstream attempts | Descriptive | **97.3 %** (rep 4) to **100 %** (reps 1–3, 5–6) | Reported |

**The attempts identity needs stating precisely.** Its denominator is
`cache_misses`, which is **zero** in five of six repetitions — once the 6 000
names are cached nothing ever misses again, every hit is stale, and all upstream
work is refresh. The ratio is undefined there, not violated. What holds instead
is the stronger equality `attempts == swr.completed + swr.failed`, exactly, in
those five. Repetition 4 has 3 050 misses from the post-restart cold cache and
evaluates the identity directly: `(114 050 − 111 000) / 3 050 = 1.000000`.

### What this closes

L.1's `swr.failed == 0` / `swr.dropped == 0` rows were vacuous — nothing was
enqueued, so passing them was arithmetic, not evidence. L.1s enqueues 114 000
refreshes per repetition and they still hold. The open question carried from
`p2.6-10` — whether the SWR path needs its own arm, since ~69 % of deployed
upstream attempts are refreshes the L.1 workload never produced — is answered:
**it needed one, and Stage 1 is clean on it.**

Two limits, stated rather than glossed:

- This inverts the deployed mix rather than reproducing it. L.1 was ~100 %
  client forwards; L.1s is 97–100 % SWR. The real ~69 / 10 blend is measured by
  neither. Each path is now covered in isolation; their interaction is not.
- `probes == 0` on the `adaptive` arm shows refresh traffic never *claimed* a
  probe against healthy endpoints. It is not evidence about SWR behaviour
  against a *failing* endpoint — that is B.8's scenario, covered in-process by
  `p2.6-09` at 100 stale keys and not on-device at load.

**L.1s: PASS.**

## L.4a — results

One run, `2026-08-25T16:46:27Z` → `17:46:28Z`, probe container, `adaptive`,
`198.51.100.53:53` first in config order. 3 600 s of strictly sequential load,
**3 365 945 queries** at 935 QPS, **0 client timeouts**.

**The probe reports `0.2.19`, production runs `0.2.20`.** The bump commit
`1c430aa` touches `Cargo.toml` and `Cargo.lock` only — no code — so the binary
under test here is the same Stage 1 code the soak runs, under the version string
the probe image was built with. Stated because a version mismatch in a results
file is otherwise a defect.

| Endpoint | state at T1 | attempts | failures | penalties | probes | probe successes |
| --- | --- | --- | --- | --- | --- | --- |
| `198.51.100.53:53` (dead) | penalized | **16** | 16 | **15** | **14** | 0 |
| `192.168.10.10:5301` | healthy | 3 365 945 | 0 | 0 | 0 | 0 |
| `192.168.10.10:5302` | healthy | 0 | 0 | 0 | 0 | 0 |

The arithmetic closes exactly: **2** attempts arm the first penalty
(`penalty_failures = 2`), then **14** probe carriers, one per window —
2 + 14 = 16 attempts, 1 + 14 = 15 penalties. Mock B was never reached, which is
first-healthy-wins stopping at A.

### The client-side view — the same 16, independently

`seq.jsonl` holds one line per query. Exactly **16** of 3 365 945 exceeded
200 ms, and every one of them cost ~804 ms — one `timeout_ms = 800` attempt plus
the healthy answer:

| # | offset (s) | cost (ms) | Window before it (s) | Nominal | Ratio |
| --- | --- | --- | --- | --- | --- |
| 1 | 0.000 | 803.7 | — | — | — |
| 2 | 0.804 | 804.0 | — | — | — |
| 3 | 22.006 | 803.0 | 20.4 | 24 | 0.85 |
| 4 | 58.807 | 804.0 | 36.0 | 48 | 0.75 |
| 5 | 169.048 | 804.3 | 109.4 | 96 | 1.14 |
| 6 | 352.249 | 803.5 | 182.4 | 192 | 0.95 |
| 7 | 584.048 | 803.9 | 231.0 | 300 | 0.77 |
| 8 | 887.847 | 803.6 | 303.0 | 300 | 1.01 |
| 9 | 1149.647 | 804.6 | 261.0 | 300 | 0.87 |
| 10 | 1510.444 | 804.0 | 360.0 | 300 | 1.20 |
| 11 | 1778.243 | 803.3 | 267.0 | 300 | 0.89 |
| 12 | 2061.043 | 803.1 | 282.0 | 300 | 0.94 |
| 13 | 2298.843 | 803.5 | 237.0 | 300 | 0.79 |
| 14 | 2674.640 | 803.5 | 375.0 | 300 | 1.25 |
| 15 | 3044.439 | 803.5 | 369.0 | 300 | 1.23 |
| 16 | 3345.238 | 803.5 | 300.0 | 300 | 1.00 |

Client-side percentiles over the whole hour: **p50 0.996 ms, p99 1.788 ms, max
804.6 ms**. Server-side mean `forward` 0.641 ms over 3 365 945 samples.

### The ladder ships with ±25 % jitter — nominal is not what runs

Every one of the 14 windows lands inside **0.75–1.25× nominal**, two of them on
an edge (0.75 and 1.25). `health.rs` applies jitter to the penalty deadline and
its own tests assert that band (`assert_in_jitter_band`). Recorded because the
phase's shorthand — "the 24/48/96/192/300 s ladder" — names the nominal, and a
reader checking a window against it on-device would find it off by up to a
quarter and think the ladder broken. `attempt_bound_ms` is 2 400 ms here
(3 × `timeout_ms`), so `penalty_base` is 24 s, and the cap holds from window 5.

### Attempts identity

Σ attempts = 16 + 3 365 945 = **3 365 961**; `swr.completed + swr.failed = 0`;
`cache_misses = 3 365 945`. The ratio is **1.0000048**, not 1.000, and the excess
is exactly **16** — the dead endpoint's failed attempts. The identity holds at
1.000 only when nothing fails, which is why L.1/L.2 could gate on it and this arm
cannot. Reported, not gated.

### `penalized_seconds_total` counts scheduled, not elapsed, time

`3 660 s` accrued across a 3 630 s run. The counter adds the whole scheduled
window when the penalty lands rather than accumulating time actually spent
penalized, so it can exceed wall-clock and cannot be read as "time this endpoint
was unavailable". Not a criterion here; flagged because the name invites the
other reading.

### L.4a acceptance

| Row | Criterion | Result |
| --- | --- | --- |
| B.1 shape on real hardware | `penalty_failures` non-probe queries pay, then one probe carrier per window | **PASS** — 2 then 14, matched independently by client latency and by counters |
| `attempt_bound_ms` cap | Report only | Not exercised: plain UDP mocks, no TC retry, every attempt cost one 800 ms leg |
| Saving vs `fallback` | Descriptive, derived | Under `fallback` all 3 365 945 queries pay the dead entry first: p50 would be ~804 ms instead of **0.996 ms**. **Derived from this arm's counters, not measured** — no `fallback` arm was run, by declaration |

## L.4b — results

One run, `2026-08-25T18:18:00Z` → `19:18:01Z`, same container and mocks,
`192.168.10.9:53` first in config order. **3 085 405 queries** at 857 QPS, **0
client timeouts**. `192.168.10.9` is below the DHCP pool, has no lease and no ARP
entry, and nothing answered there for the whole hour.

### The classification — the question this arm exists to answer

**The kernel reported a timeout, not `EHOSTUNREACH`. The path-failure row is
recorded UNCONFIRMED on this platform.**

Evidence, from a 5 s check before the measured hour, with the endpoint at round 0:

| Endpoint | attempts | failures | penalties | round | client cost |
| --- | --- | --- | --- | --- | --- |
| `192.168.10.9:53` | **2** | 2 | 1 | 1 | 804.5 ms, then 804.7 ms |

The declared criterion is **1** attempt with `failures == 1`. This took 2, each
paying a full `timeout_ms = 800` leg — the same shape L.4a produced against a WAN
black hole. Over the whole hour not one failed attempt returned in under 800 ms.

**Why, narrowed rather than assumed.** The upstream UDP socket *is* connected
(`upstream/plain.rs:64`), so an ICMP host-unreachable would be delivered to it as
a socket error; "an unconnected socket swallows ICMP" is ruled out by the code.
The second query, 0.8 s after the first, paid the same 804 ms, so no negative
neighbour entry produced a shortcut either. What remains is that the router
**drops silently when its ARP fails** rather than emitting ICMP host-unreachable.

**No second address was tried.** Choosing addresses until one classifies the
declared way is a search for a pass, not a measurement. Confirming this row needs
a topology that actually returns ICMP unreachable — future work for whoever owns
the path-failure row, not a re-run of this arm.

### Window behaviour — confirmed at a second dead address

Counters over the measured window (the arming pair fell in the pre-run check, so
every attempt inside T0→T1 is a probe):

| Endpoint | state | attempts | failures | penalties | probes | probe successes |
| --- | --- | --- | --- | --- | --- | --- |
| `192.168.10.9:53` | penalized | 14 | 14 | 14 | 14 | 0 |
| `192.168.10.10:5301` | healthy | 3 085 405 | 0 | 0 | 0 | 0 |
| `192.168.10.10:5302` | healthy | 0 | 0 | 0 | 0 | 0 |

Client-side: exactly **14** queries of 3 085 405 exceeded 200 ms, each ~804 ms —
one probe carrier per window, no other query paid. The 13 windows between them,
against nominal from round 1 (48, 96, 192, then the 300 s cap):

| Window (s) | 41.8 | 96.0 | 205.4 | 264.0 | 321.0 | 327.0 | 249.0 | 369.0 | 300.0 | 300.0 | 300.0 | 303.0 | 261.0 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Ratio | 0.87 | 1.00 | 1.07 | 0.88 | 1.07 | 1.09 | 0.83 | 1.23 | 1.00 | 1.00 | 1.00 | 1.01 | 0.87 |

All 13 inside the ±25 % jitter band, matching L.4a at a different address.

Latency: **p50 1.03 ms, p99 3.296 ms**, max 804.7 ms client-side; server-side mean
`forward` **0.718 ms**. The p99 is ~1.8× L.4a's, the one figure that differs
between the arms; a LAN destination whose ARP is being retried is the obvious
suspect and it was not chased — nothing gates on it.

Attempts identity: Σ attempts = 14 + 3 085 405 = **3 085 419** against
`cache_misses = 3 085 405`, excess exactly **14**, `swr` all zero. Same reading as
L.4a — the identity holds at 1.000 only when nothing fails.

### L.4b acceptance

| Row | Criterion | Result |
| --- | --- | --- |
| Path-failure classification | Penalized after exactly 1 attempt, `failures == 1`, `penalty_round == 1` | **UNCONFIRMED on this platform** — 2 attempts, both full timeouts. The declared fallback outcome, not a failure of the implementation |
| Corroboration | ms (path failure) vs ~1.6 s (two 800 ms timeouts) | Timeout confirmed on both counters and latency; they agree |
| Window behaviour | One probe carrier per window | **PASS** — 14 probes, 14 windows, all inside the jitter band |

### The classification gap — deferred, owner decision 2026-08-25

**L.4b is recorded as split: window behaviour PASS, path-failure classification
UNCONFIRMED.** The owner's ruling, carried here verbatim in substance:

| Ruling | Consequence |
| --- | --- |
| The observed timeout is **not** classified as `PathFailure` | The arm reports what the kernel returned, nothing more |
| `EHOSTUNREACH` is **not** inferred from the ARP failure | An ARP that fails and a socket error that arrives are different facts |
| No runtime code change | Nothing about Stage 1 is implicated by this result |
| No further random LAN-address experiment on this topology | The topology, not the address, decides whether ICMP comes back |
| **Deferred to `p2.6-12` / future topology-specific validation** | **Not a Stage 1 implementation failure** |
| The rest of `p2.6-11` acceptance proceeds independently | This gap gates nothing else in the task |

**Coverage as it now stands:** `EHOSTUNREACH` → `PathFailure` has **unit coverage
only** on this platform (`p2.6-04`, synthetic `io::Error`); the **14-window
penalty behaviour is validated on-device**, twice, at two dead addresses. The
plan's claim that L.4b is "the only real path-failure coverage in the whole plan"
is unmet and stays open.

### Probe restored, 2026-08-25

| Step | Evidence |
| --- | --- |
| Container stopped by the owner | Requested before the config write — the config is read at boot, so a live swap would have been invisible until the next start |
| Config restored | The 424 B file pulled **before** L.4a touched anything, pushed back and read off the device again: identical to that pull **and** to the committed [p2.6-10-session copy](p2.6-10-session/config/probe-fastadhunter.toml). Zero lines matching either dead address |
| Mocks stopped | Both CoreDNS processes on the dev box terminated |
| End state | `fah-probe` stopped, `strategy = "fallback"` in its TOML, two mock upstreams, exactly as L.4 found it |

Production was never touched: the soak ran throughout on `fah-next`.

## S1-G4 — run-length distribution

Read as the **sum of per-process `failure_runs` deltas**, never `end − start`
across a restart. Captures and procedure:
[phase2.5/s1g4-window/](../phase2.5/s1g4-window/).

| Segment | Build | Window | `1.1.1.1` attempts / failures | `failure_runs` |
| --- | --- | --- | --- | --- |
| 01 | 0.2.18 | 2026-08-22T22:19:31Z → 08-23T14:54:35Z (16.6 h) | 18 780 / 1 | `[1,0,0,0]` |
| 02 | 0.2.19 | 08-23T14:59:52Z → 08-24T08:28:51Z (17.5 h), **truncated** | 12 287 / 1 | `[1,0,0,0]` |
| 03 | 0.2.20 | 08-24T21:35:55Z → 08-25T07:47:00Z (10.2 h), **final** | 11 572 / 1 | `[1,0,0,0]` |

| Metric | Value |
| --- | --- |
| Closed runs, summed per process | **3**, all of length 1 |
| Runs of length ≥ 2 | **0** |
| Primary attempts | 42 639 |
| Failure rate | **0.0070 %** |
| Recorded window | ~44.3 h, plus ~13 h observed but unrecorded |

Segment 03's final capture was taken immediately before the opt-in restart. A
third failure landed between the 06:18 and 07:47 reads, with `9.9.9.9` taking
exactly one fallthrough attempt — the same 1:1 relationship suite T recorded.
The window closes with three isolated single losses and no clustering.

Segment 02's tail was lost: its process ran ~13 h past the last capture and the
p2.6-11 deploy stopped it with no final read. Recorded in the window's README;
the runs that closed in that gap are unrecoverable.

Three of four endpoints remain at 0–1 attempts across every segment. Under
`fallback` the primary answers essentially everything, so Stage 1 has no data
on the other three and cannot penalize or probe what never runs.

**Concurrency caveat, carried verbatim from the spec:** concurrent queries can
split one upstream outage across several short runs, so observed run lengths are
a **lower bound** on real clustering. Two length-1 runs are consistent with two
isolated losses and equally consistent with one clustered event split by
concurrency.

### Decision

**`penalty_failures` cannot be frozen from this data. The window must extend.**

Two closed runs cannot distinguish "this deployment produces only isolated
single losses" from "the sample is far too small". The spec is explicit that too
few closed runs means extend the window rather than guess the constant, so
`penalty_failures` stays at its compiled default of **2**, provisional and
unvalidated.

Sizing the extension from the measured rate: 0.0050 % over ~1 000 primary
attempts/hour is roughly one failure per 20 h. Distinguishing "no runs of ≥ 2
exist" from "too few events" needs on the order of 15–20 closed runs — **12 to
17 days** of `fallback` traffic at the current volume, not days already served.

**This collides with the opt-in flip.** Samples taken after the flip are a
different quantity — under `adaptive` attempts are throttled and exclude
`resolve_host` (spec S1.8) — and are explicitly not pooled with the `fallback`
window. The flip therefore **closes the S1-G4 window permanently at 2 closed
runs**.

**Owner decision, 2026-08-25: flip now, with S1-G4 explicitly not validated.**
The alternative — holding `fallback` for another 12–17 days — was put and
declined. The consequences are accepted rather than absorbed:

- `penalty_failures = 2` is a **provisional, empirically uncalibrated**
  compiled default. No measurement in this phase supports or refutes it.
- S1-G5 route 2 **stays open** and is not decidable from this deployment's data.
- The `fallback` run-length window closes at 2 closed runs, both length 1, and
  is not reopenable without reverting the strategy.

Segment 03 is captured immediately before the opt-in restart, so the `fallback`
window closes on a recorded boundary rather than losing its tail the way
segment 02 did.

### Why the two failure rates are not comparable

Suite T's 0.072 % and this window's 0.0050 % are both `failures / attempts` on
`1.1.1.1`, but the attempts are not the same population. Upstream traffic
composition, measured on the production container:

| Class | Suite T, 0.2.16, 45.7 h | 0.2.20, 8.7 h snapshot |
| --- | --- | --- |
| SWR refreshes | 69.1 % | **47.0 %** |
| `resolve_host` | 20.1 % | **36.5 %** |
| Client forwards | 10.3 % | **16.4 %** |

Total volume is nearly identical — 913 attempts/h then, 929/h now — so the
change is in the mix, not the load.

**This is a conclusion, not a defect in either measurement.** Neither figure is
wrong; they are rates over differently composed denominators, and pooling or
comparing them directly is a category error. The spec's "base rate documented —
0.072 % primary" should be read as scoped to suite T's composition.

What it does **not** establish: that the composition shift *causes* the 15×
difference. Telemetry carries no per-class failure attribution — by design
(S1.12), which is why B.8 exists to measure SWR-vs-client probe attribution
in-process — so the failures cannot be assigned to the class that shrank. The
mechanism is plausible and unproven.

Two limits on the table itself: it is an 8.7 h snapshot against a 45.7 h window,
and `resolve_host` is dominated by blocklist refreshes, which are bursty on
12–48 h schedules, so its share moves with sampling time.

Ruled out as explanations: `timeout_ms` is unchanged since phase 0
(`git log -S`), and p2.5-05 added event fields without touching
`upstreams[].failures` semantics.

## S1-G5 — rejection routes

| Route | Condition | State |
| --- | --- | --- |
| 1 | No partial failure exists in real traffic | **False — not triggered.** Suite T sample 1 recorded 27 partial failure events over 45.7 h. Partial failure exists |
| 2 | Only isolated losses; runs of ≥ 2 absent | **Undecided.** Consistent with the current window, unproven by it |

Route 1 is confirmed closed. Route 2 cannot be decided until S1-G4 has enough
closed runs, so **S1-G5 is not triggered today and is not cleared either**.

**Decision status, explicit:** S1-G4 and S1-G5 are **not validated** by this
task. What closes here is the pre-flip stage only — tiers 2 and 3, M.8 and the
SWR arm — with `penalty_failures = 2` carried forward as a provisional value.
Any later claim that Stage 1's constants were empirically calibrated on this
deployment would be false.

The honest position: if the extended window still shows only isolated single
losses, Stage 1 at `penalty_failures = 2` never engages on this deployment and
**not shipping is the correct outcome** — the spec says so directly. Nothing
measured so far argues against that route; nothing measured so far establishes
it either.

Note the tension the failure-rate discrepancy creates. Suite T sample 1 put the
base rate at 0.072 % with 27 partial failure events over 45.7 h; this window
gives 0.0050 %, an order of magnitude quieter, and the discrepancy is
unexplained. It decides whether S1-G4 can ever accumulate enough closed runs to
read — at suite T's rate the window fills in about a day, at this window's rate
it takes a fortnight.

## Gate on starting the soak

L.3 is 7 days on the production container. It does not start until all four
hold, each recorded here with its evidence:

| # | Precondition | State |
| --- | --- | --- |
| 1 | The 0.2.20 binary is healthy under `fallback` — Step 4b passed, mounts confirmed live, and Step 4c has moved `start-on-boot` to `fah-next` so a reboot inside the soak returns a resolver | **MET** |
| 2 | The opt-in is independently confirmed — `GET /api/v1/config` reads `adaptive`, and telemetry corroborates it rather than restating it | **MET** |
| 3 | L.1 / L.2 / L.1s preconditions satisfied | **MET** — all three arms ran and passed |
| 4 | M.8 executed and its acceptance table complete | **MET** — three clauses pass, F1 closed |

Step 4c was verified the hard way rather than on paper: the owner rebooted the
router at 2026-08-25T07:57:02Z and `fah-next` started on its own with the
resolver back. Had boot ownership not been moved, the reboot would have returned
no resolver at all.

## Soak — L.3

| | |
| --- | --- |
| Started | **2026-08-25T07:57:02Z**, process start after the router reboot |
| Ends | 2026-09-01T07:57Z (7 days) |
| Build | 0.2.20, container `fah-next`, `veth1` |
| Opt-in | `FAH__DNS__UPSTREAMS__STRATEGY=adaptive` via envlist `fah-optin`, visible in the container start line |
| Confirmed | `GET /api/v1/config` → `strategy: "adaptive"`, `penalty_failures: 2`, `timeout_ms: 800` |
| T0 | All four endpoints `healthy`, `penalties` 0, `probes` 0; `1.1.1.1` 111 attempts; all `swr.*` 0 |

Pass/fail is the RSS half-to-half drift over the final third of each 24 h
window, < 2 MB. `penalties` (G4 false-penalty definition), `swr.*` deltas and
measured `/data` growth are reported, not gated.

**`upstreams[].attempts` is not compared across the flip.** Under `adaptive` it
excludes `resolve_host`, which the pre-flip composition measured at **36.5 %**
of attempts, so a drop of roughly that order is the accounting change and not
the feature. Any comparison that ignores this reads a spec-mandated exclusion as
a regression.

M.8 (dev box) and the L.1/L.2/L.1s arms (probe container) touch neither the
production container nor its config, so they can run while the 0.2.20 binary
soaks under `fallback` — but none of them may follow a change to the frozen
declarations above.

### Interim readings — days 1–5 of 7

**Interim only. Decides nothing.** The soak runs to 2026-09-01T07:57Z and its
acceptance is evaluated then, over the full 7 windows. Read-only pulls —
`/health`, `/api/v1/telemetry`, `/api/v1/history/perf` — no config write.

| Pull | Perf rows through | Samples | `uptime_seconds` |
| --- | --- | --- | --- |
| 1 — 2026-08-26 | `2026-08-26T07:27:40Z` | 236, `stride 1` | 84 748 |
| 2 — 2026-08-27 | `2026-08-26T23:57:40Z` | 401, `stride 1` | 167 094 |
| 3 — 2026-08-28 | `2026-08-28T05:27:40Z` | 696, `stride 1` | 250 511 |
| 4 — 2026-08-29 | `2026-08-28T23:27:40Z` | 876, `stride 1` | 315 123 |

No restart across the four pulls: uptime advances by the wall-clock gap.

#### The declared gate — half-to-half drift over each final third

| Window | Status | Half 1 | Half 2 | **Drift** |
| --- | --- | --- | --- | --- |
| W1 | complete, 240 samples | 54.872 MiB | 54.358 MiB | **−0.514 MiB** — pass |
| W2 | complete, 240 samples | 61.269 MiB | 60.722 MiB | **−0.547 MiB** — pass |
| W3 | complete, 240 samples | 60.650 MiB | 66.352 MiB | **+5.702 MiB — FAIL** |
| W4 | partial at pull 4, 156 samples | — | — | — |

Pull 1 reported −0.653 MiB over a 23.51 h span that stopped 29 min short of the
W1 boundary. **Superseded by the −0.514 MiB above**, recomputed from the same
on-disk rows over the exact window. Neither figure is a verdict.

W3 read as partial at pull 3 with 216 samples and no final-third figure; the
remaining 24 rows carried the rise that fails the rule.

**Retracted:** an earlier interim entry asserted that the declared metric could
not observe day-to-day growth, on the argument that each final third falls in
the overnight trough. **W3 disproves it** — the metric caught the excursion at
+5.702 MiB. W1 and W2 passed because those nights were flat, not because the
window placement is blind. The gate stands as declared and is not modified.

#### The same rows, measured any other way

| Window | floor | ceiling | first 4 h | last 4 h | first-half → second-half |
| --- | --- | --- | --- | --- | --- |
| W1 | 41.92 | 62.94 | 50.68 | 54.36 | **+1.68 MiB** |
| W2 | 51.86 | 71.32 | 54.59 | 60.72 | **+2.90 MiB** |
| W3 | 54.84 | 81.52 | 58.73 | 62.38 | **+6.41 MiB** |
| W4 (partial) | 55.37 | 75.06 | — | — | **−5.58 MiB** |

W1's floor is the cold start. Floor steps: +9.94 (W1→W2), +2.98 (W2→W3),
**+0.53 MiB (W3→W4)**. The W4 ceiling falls 81.52 → 75.06 MiB.

#### Not load-driven, and not monotonic

| Window | queries | RSS floor | RSS ceiling |
| --- | --- | --- | --- |
| W1 | 136 123 | 41.92 MiB | 62.94 MiB |
| W2 | 88 418 | 51.86 MiB | 71.32 MiB |
| W3 | 79 068 | 54.84 MiB | 81.52 MiB |
| W4 (partial) | 23 251 | 55.37 MiB | 75.06 MiB |

Through W3 the series rose while traffic fell — 42 % fewer queries in W3 than
W1, ~13 MiB more resident — so it does not track query volume. W4 then
recovered: **RSS −8.66 MiB, `residual_bytes` −7.12 MiB** against pull 3, with
the floor essentially flat.

**Interim reading of this: a transient excursion, not a confirmed monotonic
leak.** Memory demonstrably returns. Four days cannot separate a slow
oscillation with a period near a day from a genuine ratchet; the remaining
windows are the evidence that can.

#### Where the growth sits

| Quantity | Pull 1 | Pull 2 | Pull 3 | Pull 4 |
| --- | --- | --- | --- | --- |
| `process_rss` | 53.74 MiB | 59.03 MiB | **74.12 MiB** | 65.46 MiB |
| `residual_bytes` | 24.15 MiB | 30.20 MiB | **45.01 MiB** | 37.89 MiB |
| `accounted_bytes` | 29.59 MiB | 28.83 MiB | 29.11 MiB | 27.58 MiB |
| `ruleset_bytes` | 24.04 MiB | 23.99 MiB | 24.01 MiB | 24.06 MiB |
| `cache_estimated_bytes` | 4.54 MiB | 3.86 MiB | 4.17 MiB | 2.81 MiB |
| `stats_aggregates_bytes` / `stats_clients_bytes` | 0.50 / 0.50 | 0.47 / 0.50 | 0.42 / 0.50 | — |
| `process_rss_anon` | 46.45 MiB | 51.68 MiB | **66.77 MiB** | 58.11 MiB |
| `process_rss_file` | 7.29 MiB | 7.35 MiB | 7.35 MiB | — |
| `minor_page_faults` | 381 630 | 821 098 | 1 175 859 | 1 535 059 |
| `process_peak_rss` | 137.17 MiB | 142.80 MiB | 142.83 MiB | 150.61 MiB |

Every accounted component stays flat throughout; the movement is entirely in
`residual_bytes` — unattributed anonymous memory. **+20.86 MiB over pulls 1–3,
then −7.12 MiB at pull 4.** Cache holds 1 213 entries at 1.19 MiB at pull 4
(3 337 / 2.51 MiB at pull 3), 1 142 of them stale, **zero evictions
throughout**.

Two explanations remain consistent and **cannot be separated from RSS alone**:
allocator high-water retention released under later pressure, or genuine
retention that happens to have unwound. Pull 4's recovery is evidence for the
former and against a one-way leak, but one recovery is not a plateau. The
discriminator is the daily floor across the remaining windows. For scale, the
0.2.19 container this replaced was measured at 90.8 MiB (see above, M.8
section); no level reached here is unprecedented for this workload.

#### Adaptive-selection counters — reported, not gated

| Quantity | Pull 1 | Pull 2 | Pull 3 | Pull 4 |
| --- | --- | --- | --- | --- |
| `penalties`, `probes`, `probe_successes`, `penalized_seconds_total` | **0** | **0** | **0** | **0** |
| `state` / `penalty_round`, all four endpoints | `healthy` / 0 | `healthy` / 0 | `healthy` / 0 | `healthy` / 0 |
| `1.1.1.1` attempts / failures / `failure_runs` | 22 756 / 2 / `[2,0,0,0]` | 37 509 / 3 / `[3,0,0,0]` | 50 562 / 8 / `[8,0,0,0]` | 54 435 / 8 / `[8,0,0,0]` |
| `9.9.9.9` attempts | 2 | 3 | 8 | 8 |
| Both v6 endpoints, attempts | 0 | 0 | 0 | 0 |
| `swr` enqueued / completed | 19 464 / 19 464 | 32 921 / 32 921 | 43 790 / 43 790 | 47 233 / 47 233 |
| `swr` dropped / failed | **0 / 0** | **0 / 0** | **0 / 0** | **0 / 0** |
| `events_dropped` | **0** | **0** | **0** | **0** |

Eight isolated failures on `1.1.1.1`, **no run ≥ 2**, none new since pull 3 —
the same distribution the S1-G4 window showed. SWR enqueued equals completed at
every pull.

DNS counters at pull 1: pass 42 374, block 92 203, cache hits 39 116, misses
3 295, stale 19 813.

`process_peak_rss` is a monotone high-water mark and exceeds RSS by design.

**`/history/perf` cadence is 360 s on this deployment**, not 60 s: 236 samples
across 23.51 h at `stride 1`, native spacing 360 s (one 395 s gap). The
[p2.6-06](p2.6-06-telemetry-review.md) F3 per-sample byte figure is unchanged;
only its per-day arithmetic was carried at a 60 s cadence, so the 0.35 MB/day
and 1.4 MB/day figures overstate this deployment's perf series by ~6×.
**Actual `/data` growth is not inferred from this** — it needs the volume
measurement (`/file print` or `du`), which has not been taken.

Raw JSON is not committed; the final pull supersedes it.

## Implementation Summary

Completed when the task is.

## Raw data and reproduction

Committed under [p2.6-11-session/](p2.6-11-session/), 963 KB. Every figure in
this file is recomputable from it without the appliance.

| Path | Contents |
| --- | --- |
| `m8/pairNN-{post,control}.out` | Criterion stdout, 9 pairs × 2 processes. `pair00` is the discarded warm-up |
| `l1l2/repNN-{W,A,B}/` | 7 repetitions — `t0-telemetry.json`, `t1-telemetry.json`, `perf.json`, `config.json`, `meta.json`, `load-std{out,err}.txt`. `A` = `adaptive`/L.1, `B` = `fallback`/L.2 |
| `l1s/repNN-{W,A,B}/` | 7 repetitions of the SWR arm, same shape |
| `l1s/calibration/rep00-W/` | The 120 s calibration `C` that selected the declared-rate branch |
| `l4/l4{a,b}/rep01-L4{A,B}/` | The two failure-injection hours, same repetition shape |
| `l4/l4{a,b}/first60.jsonl` | 240 telemetry samples at 250 ms over the first 60 s of load |
| `l4/l4{a,b}/slow.jsonl` | Every query that exceeded 200 ms — 16 and 14 rows. The full per-query logs are 216 MB and 198 MB and are **not** committed |
| `l4/config/l4{a,b}-fastadhunter.toml` | The two probe configs, byte-identical to what the device ran |
| `tools/gen.py` | Open-loop UDP generator, single socket |
| `tools/seq.py` | Strictly sequential generator for L.4 — one query in flight, per-query latency log |
| `tools/poll.py` | 250 ms telemetry sampler for the first-penalty transition |
| `tools/collect.py` | One repetition: settle, T0, load, drain, T1, perf pull |
| `tools/m8-session.ps1` | The M.8 session driver — affinity, priority, sample size, pair order |

Counters are deltas: every quantity is `t1 − t0` for its repetition. The mock
Corefiles are the committed
[p2.6-10-session/config/](p2.6-10-session/config/) copies, byte-identical.

Not committed: `coredns.exe` (1.14.7 `windows/amd64`, upstream binary), the
generated query files, and the API keys. The query files are regenerable — 6 000
names `swr-%06d.bench.invalid` for L.1s, 300 000 `rNN-%06d.bench.invalid` per
repetition for L.1/L.2.

## Findings

Empty until `start code review`.

## Status

**IN PROGRESS.**
