# p2.5-09 — Phase Verification

## Implementation Summary

Verification of phase 2.5 on the dev box and the RB5009 against the plan's
V1–V7 matrix. Two facts changed the plan on contact:

- The live "0.2.17" (`fastadhunter-rosready-0.2.17.tar`, router file date
  2026-08-17 04:11) predates every phase-2.5 commit (first: `8ffbcac`,
  2026-08-22 02:27). Live `/telemetry` carried no `counters.dns.answers` and
  no `failure_runs`; live WS events carried `upstream: null` and no
  `endpoint`. S1-G4 collection had **not** started. The workspace was bumped
  to **0.2.18** (`Cargo.toml`, `Cargo.lock`) so the first phase-2.5 deploy is
  distinguishable from the phase-2 build; `docs/project-state.md` corrected
  (owner-approved).
- V5b was unsatisfiable on the code as found: per-query `endpoint` never
  reached the WS feed (Findings F1). Fixed by p2.5-10 (`dd11dc7`), which the
  0.2.18 image carries; V5b is **PASS**, observed live, with the fix owned by
  p2.5-10.

Artifacts: `fastadhunter-rosready-0.2.18.tar` (repo root, gitignored;
13,756,416 B; legacy docker-archive; sha256
`d9ca2800d79d4749560411704bff681f8ddada64550773b4eff2591217b1269b`), built from
`0573bfb` + the version bump + the p2.5-10 wire fix via
`buildx --platform linux/arm64` and skopeo. (A first build without p2.5-10,
sha256 `29873d1e…`, was discarded before upload.)

Phase base for every PRE/POST comparison: `465e5d3` (`v0.2.17-phase2`), in a
detached worktree with its own `target/`.

## Verification matrix

| # | Check | Status | Evidence |
| --- | --- | --- | --- |
| V1a | Gates | **PASS** | `fmt --check` clean; `clippy --all-targets -D warnings` clean; `test --all-features --workspace` 979 passed, 3 ignored, 42 suites |
| V1b | Benches vs phase base | **PASS** | §Benchmarks — every arm within 10 %; control spread up to ±8.5 % |
| V1c | Config surface | **PASS** | `git diff 465e5d3..HEAD -- crates/fah-config/src/schema` empty; `lib.rs` adds only the `MAX_UPSTREAM_SERVERS` validation cap; CONFIGURATION.md delta is comments |
| V2 | Deploy | **PASS** | owner replaced the container 2026-08-23 01:19 EEST; `/health` `{"status":"ok","version":"0.2.18","uptime_seconds":62}` at 22:21:11 UTC; T0 set in `soak-0.2.18/p2.5-09-t0-0.2.18-*` (§Live device, T0) |
| V3a | Healthcheck, staging | **PASS** | §Healthcheck (staging) |
| V3b | Healthcheck, live | **FAIL** — environment | `/container/print detail` on RouterOS 7.21.5 has no health field before or after the deploy (`…-container-print-detail.txt`); RouterOS's reaction to unhealthy: none, and no state to read. The pass condition "health state populated" cannot be met on this RouterOS; F3 |
| V4 | Refresh-integrity drill | **PASS** with one deviation (refresh-all used; §V4 result) | rejected on API + log, nothing written, ruleset unpoisoned; `DELETE` returned the state to 16 lists, `/data/lists` byte-identical to pre-delete, ruleset 751,677 |
| V5a | Outcome counters | **PASS** | T0 `/telemetry` `counters.dns.answers` `{servfail_synthesized:0,servfail_relayed:0,refused_relayed:0}` |
| V5b | Endpoint attribution on WS | **PASS** via p2.5-10 (F1) | 120 s capture, 96 events: 9 forwarded events carry `"endpoint": 0`, 0 of the cache-hit events carry the key (`…-t0-0.2.18-ws-events.jsonl`); the phase-2 build showed neither (pre capture). Observed through the p2.5-10 fix, whose own code review is still open |
| V5c | Run-length buckets | **PASS** (trivially monotone) | T0 22:21:11Z and T1 22:35:58Z (14.8 min apart): `failure_runs` `[0,0,0,0]` on all four upstreams in both; `1.1.1.1` attempts 449, failures 0 — no run closed in the window, so non-decreasing holds with zero motion. Live bucketing under real failures is still unobserved; the S1-G4 window will show it. Raw: `…-t1-0.2.18-telemetry.json` |
| V5d | S1-G4 window start | **PASS** | **2026-08-22T22:19:31.808698Z** (process start, container log `…-t0-0.2.18-container-log.txt`); listeners bound 22:19:34.34Z |
| V6 | ≥ 12 h soak | **PASS** | 15.81 h from V5d, read 2026-08-23 14:12 UTC; four criteria met; §Soak result (V6) |
| V7 | Records | **PASS** | this file; raw JSONs in `soak-0.2.18/` |

Staging and live figures are never merged in one table.

## Benchmarks (V1b) — dev box, Windows, `--release`

Method: p2.5-05 §P1 recipe. Base `465e5d3` worktree vs HEAD (`0573bfb` +
bump). Bench executables resolved via `cargo bench --no-run
--message-format=json`. Pinned `ProcessorAffinity=4`, `High` priority, 200
samples (throughput: affinity 15, 100 samples). Interleaved base-A, post-A,
base-B, post-B per filter. Point estimates below are criterion's middle value;
delta = post mean of (A,B) vs base mean of (A,B). Raw outputs:
`<scratchpad>/bench/*.txt` (24 files).

Control arm first (`fah-rules` `matcher_lookup`, untouched by the phase):

| Bench | base A | base B | post A | post B | same-binary spread | delta |
| --- | --- | --- | --- | --- | --- | --- |
| `hit_exact` | 68.27 ns | 64.39 ns | 67.14 ns | 63.38 ns | 6.0 % | −1.6 % |
| `hit_subdomain` | 218.54 ns | 200.71 ns | 212.52 ns | 206.38 ns | 8.5 % | 0.0 % |
| `miss` | 50.87 ns | 47.46 ns | 48.35 ns | 47.73 ns | 7.0 % | −2.3 % |

**Deltas under ~8 % are noise on this box tonight** (worse than p2.5-05's ±5 %
— the docker arm64 build had finished but the box was not otherwise idle).

| Bench | base A | base B | post A | post B | delta | verdict |
| --- | --- | --- | --- | --- | --- | --- |
| `fah-metrics` record cache-hit pass | 17.70 ns | 17.50 ns | 18.22 ns | 17.91 ns | +2.6 % | noise band |
| `fah-metrics` record blocked | 14.37 ns | 13.94 ns | 14.67 ns | 14.35 ns | +2.5 % | noise band |
| `fah-metrics` record mixed | 18.35 ns | 18.22 ns | 18.71 ns | 18.28 ns | +1.1 % | noise |
| `stats_record/blocked` | 706.97 ns | 714.52 ns | 746.75 ns | 746.78 ns | +5.1 % | inside band; see note |
| `stats_record/pass_cache_hit` | 375.96 ns | 380.79 ns | 376.94 ns | 386.11 ns | +0.8 % | noise |
| `stats_record/mixed` | 504.55 ns | 530.48 ns | 504.51 ns | 523.30 ns | −0.7 % | noise |
| `stats_record/http` | 130.57 ns | 138.88 ns | 129.07 ns | 139.79 ns | −0.2 % | noise |
| `stats_record/harness_event_build` | 105.26 ns | 109.79 ns | 107.58 ns | 114.53 ns | +3.3 % | harness drift |
| `dns_cache/cache_hit_in_engine_latency` | 2.584 µs | 3.466 µs | 2.965 µs | 2.472 µs | −10 % | noise (own CI ±15 %) |
| `full_pipeline/blocked_query` | 3.074 µs | 3.296 µs | 2.919 µs | 3.084 µs | −5.8 % | noise |
| `full_pipeline/forwarded_query_overhead` | 4.324 µs | 4.245 µs | 4.235 µs | 4.381 µs | +0.6 % | noise |
| `full_pipeline/sustained_throughput` | 342.39 µs | 346.26 µs | 344.02 µs | 346.80 µs | +0.3 % | noise |

Note on `stats_record/blocked`: both post runs sit at 746.8 ns with ±0.3 %
CIs against 707–715 ns base, while the harness arm moved +3.3 % and `mixed`
(which contains blocked events) moved −0.7 %. Not attributable with this
evidence; below the 10 % rule either way. `fah-stats` source delta vs base is
four lines (`failure_runs` copy in the perf row, `answers_delta`).

Bench-source caveat (plan V1b): `cache.rs` and `pipeline.rs` bench forwarders
changed return type (`Message` → `ForwardOutcome`) per p2.5-05; arm names and
workloads are unchanged, compared like-for-like. `upstream.rs` is new in
p2.5-07 and has no base arm — not compared.

## Healthcheck (staging, V3a) — dev box, 0.2.18 release binary

| Case | Config | Exit | Elapsed | Output |
| --- | --- | --- | --- | --- |
| live listener | `dns.listen 127.0.0.1:15353`, engine running | 0 | — | stdout `healthcheck ok (<path>)` |
| dead port | `dns.listen 127.0.0.1:15399`, nothing bound | 1 | 48 ms | stderr `healthcheck failed: dns-probe: no reply from 127.0.0.1:15399: … (os error 10054) — [dns.listen] is a boot setting …` |
| broken TOML | `[dns.cache] max_entrees = 500` | 1 | — | stderr `healthcheck failed: config: failed to parse config: … unknown field `max_entrees`` |

48 ms is Windows' ICMP port-unreachable fast path; the 2 s read timeout is the
bound on Linux/RouterOS. Within the plan's 3 s either way.

## Live device, pre-deploy (0.2.17, phase-2 build) — RB5009, RouterOS 7.21.5

Read-only observations, 2026-08-22 19:37 UTC (T-pre snapshots in
`soak-0.2.18/p2.5-09-pre-0.2.17-*.json` — every raw snapshot of this task
lives in that directory):

- `/health` `{"status":"ok","version":"0.2.17","uptime_seconds":2765}`;
  container restarted 2026-08-22 18:50:43 UTC (clean stop/start, exit 0).
- `/telemetry`: `counters.dns` has no `answers`; upstream rows have no
  `failure_runs`. 753,317 rules, 16 lists all `ok`.
- WS `query` events (6 captured in 20 s): `upstream: null`, no `endpoint`, no
  `answer` key.
- `/container/print detail`: no health field of any kind on 7.21.5. The
  image's `HEALTHCHECK` has existed since phase 0 (`ea597b2`), so "no health
  column" is RouterOS behaviour, not a build difference. V3b after V2 will
  re-check and record.
- No `dns-adguard`/`dns-fah` scripts, no `fah-liveness` scheduler; netwatch
  DNS failover entry is **disabled** — a redeploy leaves the LAN without DNS
  for the `extracting` phase.

## Live device, T0 (0.2.18) — RB5009, RouterOS 7.21.5

Snapshots 2026-08-22 22:21:11 UTC, 62 s after start, same config and 16
lists as the pre capture (753,317 rules compiled from cache).

| Figure | pre (0.2.17, 46 min up) | T0 (0.2.18, 62 s up) |
| --- | --- | --- |
| `process_rss` | 50,085,888 | 49,070,080 |
| `residual_bytes` | 23,200,496 | 22,642,660 |
| `process_peak_rss` | 97,484,800 | 95,006,720 |
| `ruleset_bytes` | 25,219,420 | 25,219,420 |
| RouterOS `memory-current` | 47.7 MiB | 86.8 MiB (page cache from the image extract; cgroup accounting, measurement-traps §Memory) |

tui-monitor spot check (plan §Constraints): owner screenshot
`soak-0.2.18/tui-t0-0.2.18.PNG` at 01:23 EEST — `WS ONLINE`, `v0.2.18`, live
feed rendering events that now carry `endpoint`, all four upstream rows, no
error. Serde-default compatibility confirmed live.

No memory step across the deploy. The `/history/perf` series (on `/data`)
is continuous across it: 240 items pre, the T0 pull carries the same rows
plus the first 0.2.18 samples — the V6 window reads from this one series.

## V4 procedure (prepared, not run)

Disposable list: `https://example.com/` (serves an HTML document, ~1.2 kB).
Each mutation gets its own owner yes.

1. Baseline: `GET /api/v1/telemetry` (`ruleset.rules`), `GET /api/v1/lists`
   (per-list `rules_total`, `last_status`); owner: `/file/print where
   name~"fastadhunter/data/lists"` (sizes + dates).
2. **Yes #1** → `POST /api/v1/lists` `{"id":"p2509-drill","url":"https://example.com/","enabled":true,"refresh_hours":24}`
   then `POST /api/v1/lists/refresh`.
3. Verify: `GET /lists` shows `p2509-drill` `last_status: "rejected"`, every
   other list unchanged; `ruleset.rules` unchanged; `/file/print` sizes
   unchanged; no `p2509-drill` file under `/data/lists` (no baseline → nothing
   written).
4. **Yes #2** → `DELETE /api/v1/lists/p2509-drill`; re-read `GET /lists`,
   `/telemetry`, `/file/print` — back to step-1 values.

Caveat: a brand-new list has no last-good copy, so "last-good survives" is
exercised as "nothing is written and the serving ruleset is untouched". The
stronger variant (poison an existing list's source) is a config mutation on a
serving list and is not proposed.

## V4 result (owner ran the two calls at 01:30 EEST, 2026-08-23)

Raw: `soak-0.2.18/p2.5-09-v4-after-refresh-{lists,telemetry}.json`,
`…-file-print-lists.txt`; owner's container log excerpt in chat.

| Check | Result |
| --- | --- |
| `POST /lists` | created `p2509-drill`, `last_status: "never"` |
| `POST /lists/refresh` | `{"refreshed":16,"failed":1}`; drill `{"status":"rejected","error":"rejected: html document"}`; all 16 real lists `ok` |
| `GET /lists` drill row | `last_status: "rejected"`, `last_error: "rejected: html document"`, `rules_total: 0`, `last_refresh: null` |
| `/data/lists` | no `p2509-drill` file; nothing written for a rejected body |
| Container log | `WARN fah_rules::lifecycle: scheduled list refresh failed list=p2509-drill error=rejected: html document`, then `refreshed all lists lists=17 refreshed=16 failed=1 rules=751677` |
| Serving ruleset | 751,677 rules — all from the 16 real lists, zero from the drill |

**Deviation (mine):** the procedure named `POST /lists/refresh`, which
refreshes *every* list. The 16 real lists re-fetched fresh bodies (all 16
`/data/lists` files rewritten 01:31:15–20, sizes ±0.5 %), so the ruleset
moved 753,317 → 751,677 on their account and the plan's literal "rule count
unchanged" is not attributable to the drill. The correct call was the per-list
`POST /lists/p2509-drill/refresh`. What the drill does prove: a 200-OK HTML
body is refused before it can be written or compiled, the rejection is on the
API and in the log, and the serving ruleset contains only legitimately
refreshed content. The refresh-all path itself behaved as p2.5-02 specified
(one rejected beside sixteen `ok`, one batch compile).

Removal (yes #2, 01:33 EEST): `DELETE /lists/p2509-drill` → `GET /lists` 16
ids, no drill; `/telemetry` 751,677 rules (unchanged by the delete — the
drill contributed nothing); `/file/print` identical to the post-refresh
listing. Raw: `…-v4-after-delete-*`.

## Soak procedure (V6, prepared)

Window = V2 container start → ≥ 12 h later, no router reboot / ISP outage
inside it (rerun otherwise). Source `GET /api/v1/history/perf?from=<T0>&to=<T1>&fields=rss_bytes,peak_rss,memory`.
Four criteria, **all** required; any one failing fails V6:

| # | Criterion | Evaluation |
| --- | --- | --- |
| 1 | Residual: no monotone drift | `memory.residual_bytes` per sample, window split into thirds, least-squares slope per third; pass only when the slope signs disagree between thirds. A same-sign slope across all three thirds fails even if small |
| 2 | `counters.events_dropped` delta 0 | T-end `/telemetry` minus T0 |
| 3 | `counters.swr.dropped` delta 0 | T-end `/telemetry` minus T0 |
| 4 | `peak_rss` steps only at a list refresh | `peak_rss` is a lifetime high-water mark, so every sample where it exceeds the previous sample is a new peak. Each such step must sit within one sample interval (6 min) of a refresh event in the container log (`list refreshed` / `refreshed all lists`) or of the boot compile. A new peak during ordinary traffic fails — it does not matter that RSS fell back afterwards |

## Soak result (V6) — 15.81 h, 2026-08-23 14:12 UTC — **PASS**

Window 2026-08-22T22:19:34Z → 2026-08-23T14:08:08Z, 159 samples (6 min
cadence), container uptime 57,148 s continuous, no restart. Raw:
`soak-0.2.18/p2.5-09-tend12h-0.2.18-*` (history-perf, telemetry, stats,
debug-memory, lists). An owner-requested early read at 10.91 h (09:16 UTC,
`…-tend-0.2.18-*`) agreed on all four criteria and failed only on window
length.

| # | Criterion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Residual: no monotone drift | **met** | thirds slopes −0.292 / **+0.358** / −0.247 MB/h (signs disagree); third means 25.14 / 24.67 / 25.87 MB; min 14.47 (boot) max 32.93 (post-compile) MB |
| 2 | `events_dropped` delta | **met** | 0 → 0 |
| 3 | `swr.dropped` delta | **met** | 0 → 0 |
| 4 | `peak_rss` steps only at refresh | **met** | two steps in 159 samples, both attributed — 22:32:08Z 95.0 → 141.7 MB = V4 refresh-all (22:31:08–22Z); 10:38:08Z 141.7 → 143.9 MB = `phishdestroy` 12 h refresh, `lists[].last_refresh = 2026-08-23T10:32:11.878Z`, `last_status = ok`, 3.9 s after the preceding sample and inside the one-interval bound |
| — | Window length ≥ 12 h | **met** | 15.81 h |

Side checks: `hits + misses == pass + allow` holds (29,821 = 29,821, the
0.2.12 invariant); `swr` 13,211 enqueued / 13,211 completed / 0 failed;
RSS 40.8 → 54.4 MB, thirds slopes −0.179 / +0.687 / −0.195 MB/h — same
sign-alternating shape as residual, inside mimalloc's purge band.

**S1-G4 first bucketed run:** `1.1.1.1` now reports `failure_runs =
[1,0,0,0]` after 18,077 attempts / 1 failure — one closed run of length 1.
Live bucketing is confirmed working (V5c saw only zeros). One isolated
single loss and no run of ≥ 2 is, so far, the shape S1-G5's narrow
rejection route describes; a 15.8 h sample decides nothing — the window
continues on the `fallback` build.

Endpoint skew worth noting for Stage 1: `9.9.9.9` took 1 attempt and both
v6 endpoints 0 in the whole window, so three of four endpoints are
effectively unmeasured under `fallback`.

## Findings

### F1 · Major · RESOLVED by p2.5-10 · per-query `endpoint` was dropped at the API wire

Found by p2.5-09 (V5b could not be observed on the code as deployed), fixed
by p2.5-10 (`dd11dc7`): `QueryItemResponse.endpoint` + wire test, shipped in
the 0.2.18 image, verified live at T0 — 120 s capture, 9 forwarded events
with `"endpoint": 0`, every cache hit without the key. History kept below.

Chain, verified by reading:

| Hop | Location | `endpoint` |
| --- | --- | --- |
| `UpstreamPool::forward` | `fah-dns/src/upstream/mod.rs:224` | set: walk index |
| `Resolved::forwarded` → `QueryEvent::with_outcome` | `fah-dns/src/pipeline.rs:439,466,497` | `Some(idx)` on forwarded, `None` on hit/block |
| `fah_model::QueryEvent` | `fah-model/src/query_event.rs:64` | field present, `skip_serializing_if` |
| event channel → `EventHub::publish_query` | `fah-api/src/events.rs:71` | whole `fah_model::Event` boxed into `QueryRecord` — still present |
| `From<QueryRecord> for QueryItemResponse` | `fah-api/src/wire.rs:165-215` | **never read**; `upstream: None` hardcoded |
| `encode(Event::Query)` | `fah-api/src/events.rs:103` | serializes `QueryItemResponse` → no key |

No other consumer reads it: `fah-stats` never touches `endpoint`;
`fah-metrics` reads `answer` only (`registry.rs:152`). So p2.5-05's
"endpoint attribution on the event" is true of the internal `QueryEvent` and
false of every external surface. API.md §WS (as reconciled in p2.5-08)
documents exactly that. p2.5-05's review listed "WS event stream: add optional
`answer` and `endpoint` keys" as a *doc* proposal — the code never followed.

Impact: the plan's V5b cannot be observed; the phase definition of done
("per-endpoint attribution appear … on events") is not met on any surface a
client can see. Aggregate attribution does exist: `/telemetry`
`upstreams[i].attempts` per endpoint and `counters.dns.answers`.

Options (owner decision, neither applied):

- **A — amend V5b** to an observable surface: "after a forced-fallback query,
  `upstreams[1].attempts` increments while `upstreams[0]` does not" is the
  nearest per-endpoint evidence; it is aggregate, not per-query.
- **B — small change, separate task or approved fix here**: add
  `endpoint: Option<u8>` (+ `answer`) to `QueryItemResponse` with
  `skip_serializing_if`, filled from `record.as_dns()`, and API.md §WS updated
  to the absent-means-cache-hit rule. ~10 lines, no hot-path cost (the field is
  already on the event), tui-monitor unaffected (no `deny_unknown_fields`).

Owner decision 2026-08-22: **B**, opened as
`plan/wip/phase2.5-hardening/p2.5-10-ws-endpoint-attribution.md` (strict
scope: `QueryItemResponse.endpoint` + one wire test + API.md §WS).
Implemented the same night (`dd11dc7`), shipped in the 0.2.18 image and
observed live at T0 (matrix row V5b) — **F1 closed, V5b PASS**, fix owned by
p2.5-10, whose code review is still open.

### F2 · Major · project-state.md claimed a phase-2.5 deploy that never happened

"Deployed 0.2.17 … carries p2.5-01…07, so `failure_runs` (S1-G4) is collecting"
was false: the router tar predates the phase. Corrected in this task
(owner-approved). The S1-G4 window opens at V2, not before. Root cause: version
stayed 0.2.17 across the phase, so "0.2.17 is deployed" was unfalsifiable —
hence the bump.

### F3 · Minor · RouterOS 7.21.5 exposes no container health state

`/container/print detail` has no health column despite the image's
`HEALTHCHECK`. Whether RouterOS executes it at all is unknown; to be re-checked
post-deploy (V3b). If it does not, the healthcheck's value on this device is
zero until a watchdog calls `--healthcheck` itself — worth recording in
routeros-traps.md (owner-gated).

## Status

**PASS WITH DEFERRED FINDINGS.** V1–V7 are complete; V6 closed at 15.81 h on
2026-08-23 14:12 UTC. V3b remains a known environment-level criterion failure
on RouterOS 7.21.5: the image healthcheck exists and the staging check passes,
but RouterOS exposes no container health state, so the plan's original live
pass condition is unsatisfiable on this platform (F3). This does not indicate
an application failure. Everything else PASS, with the V4 refresh-all
deviation owned above.

S1-G4 collection continues on the deployed `fallback` build past this task —
it is a Phase 2.6 input, not a p2.5-09 criterion.
