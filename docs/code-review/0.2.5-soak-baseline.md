# 0.2.5 soak baseline — RB5009

**T0 (process start):** `2026-07-25T20:50:23Z`
**Baseline snapshot:** `2026-07-25T21:05:09Z` (T0 + 15 min)
**Image:** `fastadhunter-arm64-0.2.5.tar` · `docker.io/library/fastadhunter:0.2.5`
**Contains:** p2-00 (parser correctness) · p2-07 (memory accounting) + temporary
collection-cost gauge

Read the next sample **after `2026-07-26T20:50Z`**.

---

## Why this soak exists

The previous soak could only report RSS. It showed RSS flattening, which is
consistent with "no leak" but does not prove it — 45 % of RSS was unattributed,
which is exactly where a slow leak hides. This build reports
`residual_bytes`, so the question is answerable:

> **Leak = residual growing while the named components stay flat.**
> Growth in a *component* is not a leak — it is that structure filling toward
> its cap.

## Baseline

| | Value |
| --- | ---: |
| `process_rss` | 41,324,544 (39.41 MiB) |
| `accounted_bytes` | 25,804,212 (24.61 MiB) |
| **`residual_bytes`** | **15,520,332 (14.80 MiB)** |
| `ruleset_bytes` | 23,006,471 (21.94 MiB) |
| `cache_estimated_bytes` | 257,024 · 222 entries |
| `stats_aggregates_bytes` | 524,160 |
| `stats_clients_bytes` | 66,203 |
| `query_log_ring_bytes` | 1,949,392 |
| `query_log_pending_bytes` | 962 |
| `fastadhunter_ruleset_rules` | 684,087 |
| `fastadhunter_events_dropped_total` | 0 |
| RouterOS total used | 206.40 MiB of 1024 (20.1 %) |

Cache and ring are both still filling at T0+15min — expect them to grow toward
their caps. That is not the signal.

## Lists — all 15 `ok`

1,023,132 rules parsed · 1,022,382 active DNS · **750 inactive**. No `degraded`
list, so the p2-00 misparse detector is quiet, as it should be on these sources
(mostly hosts / domain-list format rather than adblock).

## Accounting cost — the estimate was wrong

`fastadhunter_memory_collection_seconds` read **692 µs**, then **842 µs** on a
later poll.

`p2-07-review.md` §5 estimated **200–300 µs** by scaling an x86 measurement.
The real figure is **2–3× higher**. Two reasons the extrapolation was bad, both
knowable in advance:

1. The x86 test ran on Windows, where `process_rss()` returns `None` — so the
   `/proc/self/status` open+read+parse was **never in the 43 µs**.
2. Only `Stats::heap()` was measured; the deployed pass also walks the matcher
   and the cache.

Impact is still negligible: 842 µs every 10 s is **0.008 % duty cycle**. The
conclusion (keep the accounting) is unchanged — but it was reached by the wrong
route, and the number to quote from now on is the ARM one.

## What to check at T+24h

| Check | Pass |
| --- | --- |
| `residual_bytes` | flat — the leak signal |
| `process_rss` | ≤ 128 MB and not trending |
| Components | may grow toward caps, then flat |
| `fastadhunter_events_dropped_total` | still 0 |
| Watchdog restarts | none (no scheduler configured) |
| p99 block / cache-hit latency | < 1 ms |
| `memory_collection_seconds` | stable, not drifting up |

```sh
curl -s --insecure -H "Authorization: Bearer $FAH_KEY" \
  https://172.17.0.3:8443/api/v1/debug/memory
curl -s --insecure -H "Authorization: Bearer $FAH_KEY" \
  https://172.17.0.3:8443/metrics | grep fastadhunter_memory
```

## Confounds to remember

- **The router was rebooted at T0.** RouterOS total memory dropped 611.90 →
  206.40 MiB, so ~405 MiB was accumulated state (most likely page cache), not a
  baseline. It is expected to climb back; that is not FastAdHunter. FAH's own
  RSS is 39 MiB of the 206.
- **AdGuard `start-on-boot` was set to `no`** before the reboot, so it no longer
  competes for the shared 1 GB. It was already `stopped` beforehand.
- **`/stats` is clean this time.** The previous window was diluted by
  load-generator traffic (`heystanzo.com`), which made the reported block rate
  meaningless. This process started fresh with household traffic only.
- Nothing in 0.2.5 changes memory behaviour, so the router-level curve has no
  reason to differ from before the reboot.

## p2-00 verified on-device

16 of the 17 previously over-blocked domains now return `pass` via
`POST /api/v1/rules/test`. The exception is `bing.com`, blocked by
`||bing.com^` in **`user-rules`** — the operator's own rule, not the parser bug.
`doubleclick.net` still blocks, so filtering is intact.
