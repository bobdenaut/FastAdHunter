[package]
name = "fah-dns"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

# No dependencies yet — this crate is a Phase 0 stub. Its downstream edges
# (fah-rules, fah-model, fah-config, fah-common, fah-logging, per
# ARCHITECTURE.md L3) get added when the consuming code lands. The layering
# guard test (fastadhunter/tests/layering.rs) enforces direction on whatever
# edges exist.
[dependencies]
