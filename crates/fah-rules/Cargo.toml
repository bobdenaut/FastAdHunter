[package]
name = "fah-rules"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

# No dependencies yet — Phase 0 stub. Downstream edges (ARCHITECTURE.md L2) get
# added when the consuming code lands; the layering guard test enforces them.
[dependencies]
