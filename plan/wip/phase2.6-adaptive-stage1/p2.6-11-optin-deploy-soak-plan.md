# P2.6-11 — Opt-in Deploy and Soak — Implementation Plan

**Task:** [p2.6-11-optin-deploy-soak.md](p2.6-11-optin-deploy-soak.md) · **Spec:** [adaptive-upstream-selection.md](../../../docs/design/adaptive-upstream-selection.md)

The agent changes nothing on the RB5009. Every router command is proposed
with its effect and timing; the owner runs it. The task ends `AWAITING
SOAK` and the table cell names what flips it.

## Spec coverage

§Stage 1 acceptance gates (tier table; "all five decided, S1-G5 not
triggered"), S1-G2 tiers 2–3, S1-G4 (run-length read as a `failure_runs`
delta; concurrency-splitting caveat; extend the window rather than guess),
S1-G5 (both rejection routes), S1.9 (flattening counters), S1.14 (binary
first, config second; 7-day soak), S1.15 (what to expect per scenario).

## Current code location

- Release mechanics: `Cargo.toml` workspace version, the Docker build
  (`SECURITY.md` / `CONFIGURATION.md` §Deployment — read the section only),
  and the exact sequence p2.5-09 used
  (`docs/code-review/phase2.5/p2.5-09-phase-verification-review.md`,
  "the 0.2.18 deploy").
- Opt-in: env override `FAH__DNS__UPSTREAMS__STRATEGY=adaptive` (exists,
  `crates/fah-config/src/env.rs:76`) set on the container; confirm via
  `GET /api/v1/config`. `penalty_failures` stays at the compiled default
  unless S1-G4 says otherwise.
- Evidence sources (read-only): `GET /api/v1/telemetry` (`upstreams[]`
  with the p2.6-06 fields, `swr.*`, `cache.*`), `GET /api/v1/history/perf`
  (60 s samples: `upstreams[].failure_runs`, RSS), `/system/resource/print`,
  `/container/print`, `/log print` on the router.
- `docs/routeros-traps.md` — every router fact and the deploy traps
  (memory-high, container restart semantics).

## Exact files / symbols to inspect

| File | What |
| --- | --- |
| p2.5-09 review file | deploy sequence, drill procedure, telemetry extraction script if any |
| `docs/routeros-traps.md` | container start/stop and env-edit commands (owner-run), what not to propose |
| `docs/measurement-traps.md` | RSS reading (mimalloc purge band ±6 MB), rate reading, control arm |
| spec §S1-G2 tier 2 table | the six invariants and their criteria |
| spec §Failure clustering | how to read `failure_runs` deltas; open runs absent; concurrency splits runs |
| p2.6-10 review file | the tier 3 decision and metric |

## Implementation changes

No code. Sequence:

1. **Merge-gate check** (dev box): S1-G1 #1–#17 green (list the test names
   and their task of origin); p2.6-08 and p2.6-09 results files exist and
   meet their criteria. Any miss → stop; the phase table stays `WAITING`.
2. **Release proposal**: version bump commit (owner-approved), image build
   command, push to the registry the project uses, and the container
   update sequence as p2.5-09 did it — each router command listed with
   effect and timing. Deploy order: binary first; the env opt-in second,
   as a separate container edit the owner applies after the new binary is
   confirmed healthy under `fallback`.
   **Rollback note (S1.14 in reverse).** The new binary serializes every
   field, so first-boot generation and any `POST /api/v1/config` write
   `penalty_failures = 2` into `/config/fastadhunter.toml`. Rolling the
   binary back to 0.2.19 against that file fails boot —
   `deny_unknown_fields` rejects the key. A rollback proposal must therefore
   remove the `penalty_failures` line from the config file **before** the
   old image starts; state this in the rollback commands, not only in prose.
3. **T0 snapshot** after opt-in: `GET /api/v1/config` shows `adaptive`;
   `/telemetry` baseline saved to the review file.
4. **Soak, 7 days** (S1-L L.3, production container): daily read-only
   snapshots. On L.3 the tier 2 rows are **report-only** except RSS:
   `penalties` per endpoint is G4 row 5 (false penalties, by the benchmarks
   doc's definition — a delta in a 60 s sample where the other endpoint's
   `failures` did not move); `swr.*` deltas reported beside the 0.2.18
   window; RSS half-to-half drift over the final third of each 24 h window
   < 2 MB is the one pass/fail. **`upstreams[].attempts` is not compared
   across the flip**: under `adaptive` it excludes `resolve_host` (≈ 20 % of
   the 0.2.18 mix, spec S1.8), so a drop of that order is the accounting,
   not the feature — state it beside the figure.
   **Tier 2 pass/fail lives in L.1 vs L.2** (probe container, mock upstreams
   that never fail), every term a delta over the run: `penalties == 0` on
   every endpoint in both arms; `(Σ attempts − (swr.completed + swr.failed))
   / cache_misses == 1.000` in both arms; `penalties` and `probes` deltas
   == 0 in the `adaptive` arm (the 10 s poll cannot see a probe — `state` is
   decided from the counters); `swr.failed == 0` and `swr.dropped == 0` in
   both arms, `swr.completed` reported. QPS ≥ 10 000 absolute in L.1; the
   relative "no worse than L.2" uses the S1-N threshold when tier 3 is alive.
   **Tier 2 L.1/L.2 and tier 3, if alive**: both arms run in the **probe
   container** with its own config — `strategy` flipped there, the
   production container's env untouched for these arms. The p2.6-10 metric
   is compared against its frozen threshold, same session, flat control arm.
   **L.3 false penalties** use the benchmarks doc's definition: a `penalties`
   delta in a 60 s sample where the other endpoint's `failures` did not
   move; deltas where every endpoint's `failures` moved are outage
   penalties, reported separately. **L.4a / L.4b** (probe container, 1 h
   each): one entry at a routable WAN address nothing answers on (black
   hole, B.1 shape), and one entry at an unused address inside the LAN
   prefix (ARP failure → `EHOSTUNREACH`, the only real path-failure
   coverage for UDP — penalized after exactly one attempt). Both addresses
   are proposed to the owner, who confirms nothing answers there.
5. **S1-G4**: `failure_runs` over the `fallback` window — from the p2.5-06
   deploy to the opt-in flip — per endpoint, **as the sum of per-process
   deltas** read from `/history/perf`: the counters are cumulative per
   process and the window contains at least two restarts (0.2.16 → 0.2.18
   → the 2.6 binary under `fallback`), so `end − start` across a restart
   undercounts (M10). State the bucket counts; decide whether runs of ≥ 2
   exist and at what share; derive `penalty_failures` from that or state the
   window must extend (preferred over guessing). Carry the
   concurrency-splitting caveat verbatim. Samples after the flip are a
   different quantity (throttled attempts) and are not pooled.
6. **S1-G5**: route 1 (no partial failure) — already false per suite T,
   confirm; route 2 (isolated losses only, runs of ≥ 2 absent) — decide
   from step 5.
7. **Outcome record**: `docs/code-review/phase2.6/p2.6-11-optin-deploy-soak-review.md`
   with every number, its window, device, build; then propose the
   `docs/project-state.md` rewrite (owner approval).

## Tests

None new. Drills from p2.5-09 (listener death) are not repeated unless the
owner asks.

## Benchmarks / gates

S1-G2 tiers 2–3, S1-G4, S1-G5, the 7-day soak. These are the deployment
tier; passing them is the precondition for p2.6-12.

## Dependencies

p2.6-06 (fields to read), p2.6-08, p2.6-09, p2.6-10. Owner availability for
the router steps.

## Non-goals

Flipping the default; any router change by the agent; tuning
`PENALTY_MAX` (a policy constant; if the soak suggests 60 s, that is a spec
amendment for the owner, not a config change); Stage 2/3 readings of the
data.

## Open items (procedural — no spec contradiction)

- **Opt-in needs a container env edit**, which is a `/container set`
  command — on the owner's forbidden-for-the-agent list. It is proposed
  exactly and left to the owner; the plan does not assume it has been done
  until `GET /api/v1/config` shows `adaptive`.
- **Tier 3 `fallback` arm** is L.2 in the probe container (its own config,
  benchmarks §S1-L), not a production env flip — no extra `/container set`
  on the production container is proposed for it (M11). If the probe
  container is unavailable, tier 3 is reported as not run and the
  healthy-path claim rests on tiers 1–2, which the spec allows.

## Acceptance criteria

- Merge-gate check written down with the evidence list.
- Deploy and opt-in recorded with build, time, `GET /api/v1/config`.
- Every tier 2 invariant has a measured value and a pass/fail.
- S1-G4 and S1-G5 carry decisions with evidence; if the window is
  extended, the review file says what date re-evaluates it and the phase
  table cell names it.
