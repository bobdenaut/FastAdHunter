---
artifact_contract: ce-unified-plan/v1
artifact_readiness: implementation-ready
execution: code
product_contract_source: plan/wip/phase2.5-hardening/p2.5-09-phase-verification.md
---

# Plan — p2.5-09 Phase Verification

## Summary

Prove the phase on the device with objective pass criteria. Every
router-side step is proposed as exact commands the owner runs; API
mutations get a per-step owner yes.

## Verification matrix (each row has a binary pass condition)

| # | Check | Pass condition |
| --- | --- | --- |
| V1a | Gates | all four commands green, zero warnings |
| V1b | Benches | DNS pipeline + cache + stats/metrics record benches vs a `git worktree` at the phase base commit: every hot-path bench within 10 % of the base arm (bench sources differ per p2.5-05 — compare like-for-like arms only; a bench whose source changed is compared on its unchanged arms and noted) |
| V1c | Config surface | `fah-config` schema diff vs base commit shows zero removed keys and zero added TOML keys |
| V2 | Deploy | owner runs proposed upload/replace; `GET /health` = ok and `/telemetry` reports the new version; T0 snapshot set captured (`/telemetry`, `/api/v1/stats`, `/api/v1/debug/memory`) |
| V3a | Healthcheck, staging | dev-box binary + live listener → `--healthcheck` exit 0; config naming a dead port → exit 1 within 3 s, stderr contains `dns-probe`; broken config → exit 1, stderr contains `config` |
| V3b | Healthcheck, live | owner-run `/container/print` shows the health state populated; review file records RouterOS's reaction to unhealthy (expected: none automatic — the gain is the signal) |
| V4 | Refresh-integrity drill | owner approves adding one disposable list whose URL serves an HTML page; after `POST /api/v1/lists/refresh`: list status shows `rejected:`, `/telemetry` rule count unchanged, `/data` list cache sizes unchanged (`/file print` proposed to owner); disposable list removed (owner yes) and state returns to baseline |
| V5a | Outcome counters | `/telemetry` carries `counters.dns.answers.{servfail_synthesized,servfail_relayed,refused_relayed}` (zeros acceptable) |
| V5b | Endpoint attribution | one observed WS event with `endpoint` set on a forwarded query; one cache-hit event without the field |
| V5c | Run-length | `failure_runs` present per upstream; two snapshots ≥ 10 min apart show every bucket non-decreasing |
| V5d | Window | review file records the S1-G4 observation-window start timestamp (the V2 deploy time) |
| V6 | Soak | ≥ 12 h window: residual slope signs disagree between window thirds (no-drift criterion per docs/measurement-traps.md), `events_dropped` delta 0, SWR `dropped` delta 0, `peak_rss` step only at the scheduled refresh; a window containing a router reboot or ISP outage is rerun, not argued |
| V7 | Records | review file `docs/code-review/phase2.5/p2.5-09-phase-verification-review.md` with raw snapshot JSONs beside it; phase table set DONE for all nine rows |

## Sequence

V1 (dev box) → V2 (owner deploy) → V3-V5 same day → V6 overnight → V7.
The `wip` → `closed` move, tag, commit, and push are the owner's calls —
stop and report after V7.

## Constraints

- **No router changes by the agent** — V2/V3b/V4's router-side steps are
  proposed command blocks only; the two list mutations in V4 each get an
  explicit owner yes.
- Staging and live figures never merged in one table (corpus/workload/
  device scoping).
- tui-monitor spot check during V5b: the deployed monitor renders events
  without error (serde-default compatibility, verified in p2.5-05 tests,
  confirmed live here).

## Out of scope

S1-G4 analysis; phase close move; push/tag; Phase 3 preparation;
project-state.md rewrite if already done in p2.5-08 (else proposed here,
owner-gated).
