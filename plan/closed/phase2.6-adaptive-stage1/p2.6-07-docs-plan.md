# P2.6-07 — Documentation — Implementation Plan

**Task:** [p2.6-07-docs.md](p2.6-07-docs.md) · **Spec:** [adaptive-upstream-selection.md](../../../docs/design/adaptive-upstream-selection.md)

Every edit below is proposed as a diff and applied only on the owner's
explicit yes for that file (working agreement). This plan does not
authorize any of them.

## Spec coverage

S1.12 "Two semantic changes" (1: `consecutive_failures` / `/health`; 2:
measurement-traps §Traffic and rates), S1.12 §Probe and penalty semantics,
S1.13 (CONFIGURATION.md section only), root CLAUDE.md hard rule 6
(CONTEXT.md vocabulary), principle 19 (section-only edits, present tense).

## Current code location

The code these docs describe lands in p2.6-01 (config), p2.6-05 (behaviour),
p2.6-06 (telemetry). Read their Implementation Summaries, not the code.

## Exact files / symbols to inspect

| File | What to Grep for | Edit |
| --- | --- | --- |
| `CONFIGURATION.md` | `## ` heading containing `[dns.upstreams]`; the `strategy` and `timeout_ms` rows; the `at most 255` server-cap comment (line 109); the env-var naming convention used elsewhere in the file | add `adaptive` to `strategy`'s values (opt-in; default unchanged); add the `penalty_failures` row (default 2, boot-only, env `FAH__DNS__UPSTREAMS__PENALTY_FAILURES`); **correct the server cap from 255 to 8** — `MAX_UPSTREAM_SERVERS = 8` since p2.6-01 (spec S1.2), so the current text documents a limit the binary no longer enforces and a 9-server config fails boot; `timeout_ms` row gains its validated range `1..=10000` and the sentence that it bounds one leg, an attempt being bounded at `3 × timeout_ms`; one sentence that penalty durations are derived/fixed (`10 × 3 × timeout_ms` base, 300 s cap) and not keys |
| `API.md` | `upstreams` inside the `/telemetry` shape; `consecutive_failures`; `failure_runs`; `/health` and `degraded` | add the seven fields with one-line meanings; `state` is meaningful under `adaptive` only (always `healthy` under `fallback`); `penalized_seconds_total` = scheduled unavailability; probe ⊂ `attempts`/`failures`/`failure_runs`, a failed probe ⊂ `penalties`; `attempts`/`failures` exclude `resolve_host` traffic under `adaptive`; `failure_runs` under `adaptive` counts throttled attempts, not time; `consecutive_failures` diagnostic, saturates at 255 under `adaptive`; `degraded` = no endpoint `healthy` under `adaptive` |
| `docs/measurement-traps.md` | `§Traffic and rates`, the entry "Upstream `failures` as client timeouts" | append the S1.9 case: counters grow more slowly during an outage because penalized endpoints are skipped; `failure_runs` measures attempts, not duration |
| `CONTEXT.md` | existing entries for `upstream`, `endpoint`, `SWR`; the alphabetical or sectional convention | add: endpoint (one `[[dns.upstreams.servers]]` entry, `EndpointId`), Healthy / Penalized / Probing, probe (on-path primary attempt claimed by one query), penalty and penalty round, policy (Stage 1 sense: the constants, not the state machine). No Stage 2/3 terms |

## Implementation changes

Four diffs, in this order, each shown in full in chat and then applied only
after "yes" for that file:

1. CONFIGURATION.md `[dns.upstreams]` — section only.
2. API.md `/telemetry` and `/health` — rows only.
3. measurement-traps.md — one entry extended.
4. CONTEXT.md — new terms, existing entries untouched unless one
   contradicts Stage 1 vocabulary (then flag it, do not rewrite).

Every sentence is present tense and describes the shipped behaviour. No
history, no "was changed in 2.6".

## Tests

None (docs). Verification: each new term in CONTEXT.md appears in the code
or comments with the same spelling; each CONFIGURATION.md default matches
`default_penalty_failures()`; each API.md field name matches
`UpstreamSample` serde output (run the p2.6-06 serialization test and
compare).

## Benchmarks / gates

None. `cargo test` unaffected.

## Dependencies

p2.6-06 (field names final). Unblocks nothing — p2.6-08 onward do not wait
on docs — but p2.6-11's deploy note expects CONFIGURATION.md to say how to
opt in.

## Non-goals

README.md; the spec; ADRs (nothing is reversed); PERFORMANCE.md (p2.6-08
writes its numbers to `docs/code-review/phase2.6/`, and a root-doc pointer
is proposed there if one is warranted); `docs/project-state.md` (rewritten
at p2.6-11/12).

## Open items (procedural — no spec contradiction)

- None expected. If a doc currently states a default or shape that the
  p2.6-01/06 code contradicts (for instance an API.md example that lists
  `consecutive_failures` as the liveness signal), flag it in the review
  file; the fix is part of the proposed diff for that file.

## Acceptance criteria

- Four diffs proposed; each applied or explicitly declined; declined ones
  recorded as deferred in the review file.
- No file outside the four touched; no unrelated section rewritten.
